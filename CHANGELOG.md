# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

`0.x` 系のため、マイナー版の更新に後方非互換の変更を含むことがある。
バージョン 0.1.0〜0.14.0 は**開発履歴から遡及的に付与**したもの（2026-08-07 時点で整理）。

---

## [0.15.0] — 2026-08-18

**充填アニメーションを任意の位置で止めて見られるようになった。**

自動再生されるだけの GIF を、ドラッグでシークできる HTML5 プレイヤーに置き換えた。
フレーム PNG は以前から書き出していた（ZIP 同梱用）ので、レンダリングコストは増えていない。

### 追加
- `core/fill_player.py` — `build_fill_player_html()`。フレーム PNG を data URI で
  埋め込んだ自己完結 HTML プレイヤー。再生 / 一時停止、ドラッグシーク、コマ送り、
  速度切替（0.25〜2x）、ループ切替、キーボード操作（Space / ← / →）。操作は
  すべてブラウザ内で完結し、シーク中にサーバへ戻らない
- `visualizer.fill_frame_times()` / `fill_frame_fractions()` — GIF・PNG 連番・
  プレイヤーが共有するフレーム時刻の単一ソース
- `tests/test_fill_player.py`（8件）

### 変更
- UI の「充填先端アニメーション」が GIF ではなくプレイヤーを表示する。GIF は
  従来どおり ZIP に同梱（ダウンロード用途は変わらない）
- `render_fill_animation` と `export_frames` に重複していたフレーム時刻の
  `linspace` を `fill_frame_times()` に集約。3系統目のプレイヤーが同じ式を
  持たないようにするための整理

### 既知のトレードオフ
- プレイヤーは `st.components.v1.html` の iframe 内で動くため Streamlit の
  テーマ色を継承しない。明暗どちらでも読める中間色で組んである
- 全フレームを data URI で埋め込むので、既定 60 フレームで HTML が約 1.7MB になる

---

## [0.14.0] — 2026-08-07

**JSONスペックで外部からゲート形状を持ち込めるようになった。**

図面から抽出した深さ場モデルを JSON で受け取り、キャビティ形状にラスタライズする
4系統目のジオメトリ。これまでのようにビルダーをコードで書き足さなくても、
新しいゲート形状を解析にかけられる。

### 追加
- `GateProfileSpec`（JSON入出力つきスペック定義）と `build_profile_gate_geometry`
  — ランド／メインランプ（底打ちあり）／浅いアイランド／外壁線／オブラウンド井戸／
  バルブオリフィスの合成による深さ場。左右対称・片側の両モード対応
- Streamlit UI に「Profile gate (JSONスペック)」系統（デモプリセット／アップロード／貼り付け）
- 架空寸法のデモスペック `data/gate_profiles/demo_profile_gate.json`
- テスト28本（閉形式の体積検算、井戸体積の放射積分照合を含む）

### 修正
- JSON のセクション値がオブジェクトでない場合に `TypeError` が UI のエラー処理を
  すり抜けていた問題（パス付きの `ValueError` に統一）
- ゲート形状がラスタ格子をはみ出す場合に無言で切り詰められ、誤った体積・充填結果に
  なっていた問題（事前検証で明示的に拒否）

### 変更
- `well.floor_t_range` を任意項目化し、図面参照用のメタデータであることを明記
  （実際の床形状は深さと壁角から導出される）
- CI: `docs/` を ruff の対象から除外（新しい ruff が Markdown 内のコード片を
  整形対象にするようになったため）

---

## [0.13.0] — 2026-06-09

**3D表示が軽くなり、肉厚の段差が段差として正しく見えるようになった。**

### 変更
- 天面・床の描画をフルグリッドの `Surface` から、キャビティセルだけを張る
  疎な `Mesh3d` に置換 — 回転・ドラッグの負荷を大幅に削減
- 天面を「厚み一定の平らなブロック」の集合として描画するよう変更。隣接セルで
  肉厚が変わる境界には段差壁を立て、設計上の段差を垂直な段差として忠実に再現
  （従来はセル中心の補間で1セル幅の傾斜に均されていた）
- メッシュ粗さの下限を 0.5 → 0.2 mm/cell に開放。併せてセル数の上限ガードを追加
  （クラウド環境でのメモリ枯渇を防止）

### 削除
- 表示専用のアップサンプリング機能、および表示専用の解析的精細化機能
  — いずれも描画コストに見合う効果が得られず撤回

### 既知のトレードオフ
- 平らなブロックで描くため、意図的に連続なテーパ（ランナーのスロープ等）は
  微細な階段状に見える。設計段差との判別が原理的に困難なため、製品面の段差を
  正確に見せることを優先している

---

## [0.12.0] — 2026-06-07

**パラメータが増えても迷わないUIになった。**

### 変更
- 入力UIを大項目・中項目・小項目の折りたたみ階層に全面再構成。起動時は
  すべての大項目を畳んだ状態にし、必要な箇所だけ開く形に
- テーパ段化を OFF にした場合、全幅で完全に同一の単一テーパになるよう修正

### 見送り
- アクセント色のカスタマイズ — Light/Dark テーマ切替と両立できないため撤回

---

## [0.11.0] — 2026-06-06

**肉厚調整ゲート（直角台形）を扱えるようになった。**

### 追加
- 3系統目のジオメトリ「Film gate 2（ゲート位置可変）」
  — 直角台形のフィルムゲート、ゲート位置の可変化、2段テーパ、深ランナー、
  ランド境界の段差指定に対応

---

## [0.10.0] — 2026-06-05

**社外に出せる解説資料が揃った。**

### 追加
- 顧客向けの流動解析解説資料5本をリポジトリ管理下に統合
  （解析の仕組み、商用CAEとの違い、プログラム解説ほか）

### 変更
- 支配方程式の記述を実装に合わせて修正（拡散方程式 → ポアソン方程式）
- 金型冷却解析の扱いについて、資料間で線引きを統一

---

## [0.9.0] — 2026-05-15

**粘性発熱を考慮できるようになり、結果一式をまとめて持ち出せるようになった。**

### 追加
- 剪断発熱（粘性散逸）の補正モデル。ブリンクマン数を常時算出し、
  発熱の影響度を信号灯で提示
- アニメーションGIFと全フレームPNGを1つのZIPで一括ダウンロード

### 変更
- 圧縮成形（ICM）のUIをストローク指定に一本化
- 壁面冷却モデルの呼称を整理し、極薄プレート向けの既定値に調整

### 修正
- クラウド環境で材料データベースのキャッシュが更新されず、
  スキーマ変更後に不整合が起きる問題

---

## [0.8.0] — 2026-05-13

**板厚方向の温度・粘度分布を解く層別ソルバーが入った。物理エンジンの世代交代。**

これまで単層として扱っていた板厚方向を N 層に離散化し、各層の温度・粘度・剪断速度を
連成させて解く新ソルバー。壁面近傍の冷却と中央部の流動を分離して評価できる。

### 追加
- `MultilayerHeleShawSolver` — 層別の温度評価（1次元熱伝導解）、層別粘度、
  収束計算による連成。N=1 で従来ソルバーと厳密に一致
- 壁面を細かく刻む層分布、中央層の温度に基づくショートショット判定、
  収束の適応的減衰
- 層別プロファイルの可視化（温度・粘度のグリッド表示、ショートショットマップ）

### 追加（圧縮成形）
- 圧縮量をストローク（絶対量）で指定するモード
  — 段差のあるプレートで段差を保ったまま圧縮できる

---

## [0.7.0] — 2026-05-08

**ダイレクトゲートに対応し、理論的な裏付けを画面上で説明できるようになった。**

### 追加
- 2系統目のジオメトリ「Direct gate」— 製品内部に直接ゲートを配置する形式。
  プレートの2層分割にも対応
- 支配方程式と適用範囲の解説（数式表示つき）を画面上に追加
- 材料データベースにタルク強化ポリプロピレン3種（PP-T10 / T20 / T30）を追加

### 変更
- 射出率スライダーの既定値を実機相当に調整
- 画面上の表記を対外説明に耐える文体・用語に統一

---

## [0.6.0] — 2026-05-08

**結果を立体で見られるようになった。**

### 追加
- Plotly による3D可視化（肉厚マップ／充填時間／圧力）
  — パーティングライン基準の押し出し表現、側壁の着色、実寸等倍表示

---

## [0.5.0] — 2026-05-06

**誰でもブラウザから触れる公開アプリになった。**

### 追加
- Streamlit Community Cloud へのデプロイ設定と公開URL
- 解析結果（GIF等）のダウンロード機能

### 変更
- 射出率・圧縮成形の既定値を実機の運転条件に合わせて再設定
- 解析実行ボタンをヘッダー行へ移動

---

## [0.4.0] — 2026-05-06

**流動バランスの設計検討ができるようになり、壁面の凝固層を物理モデルに取り込んだ。**

### 追加
- フローバランサー（中央を意図的に薄くして樹脂を外側へ振り分ける肉盗み）。
  最大5段のネストに対応
- 壁面凝固層（スキン層）モデル — 壁から成長する固化層でコアの流路が狭まる効果を
  収束計算で評価。流路が閉塞した箇所はショートショットとして提示
- プレートをゲート側／反ゲート側の2つの肉厚帯に分割する機能

---

## [0.3.0] — 2026-05-05

**ゲート付きの実形状を入力できるようになった。**

### 追加
- 1系統目のパラメトリックジオメトリ「Film gate」
  — 台形ランナー＋半円部＋バルブゲートの平面形状を寸法指定で生成
- Streamlit UI と CLI デモの両方に配線

---

## [0.2.0] — 2026-05-05

**解析結果の正しさを自動で検証できるようになった。**

### 追加
- 1次元ストリップ流れの解析解と数値解を突き合わせる検証テスト
- GitHub Actions による CI（lint・フォーマット・テストを Python 3.11 / 3.12 で実行）

### 変更
- ruff によるコード整形を全体に適用

---

## [0.1.0] — 2026-05-05

**最初の動くシミュレータ。**

### 追加
- Hele-Shaw 近似 + Cross-WLF 粘度モデル + 疑似伝導法による充填時間場ソルバー
- 充填アニメーション、圧力マップ、ウェルドライン／エアトラップ推定
- Streamlit UI と CLI バッチ実行
- MIT ライセンスでの公開、pyproject.toml ベースのパッケージング

[0.14.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.14.0
[0.13.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.13.0
[0.12.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.12.0
[0.11.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.11.0
[0.10.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.10.0
[0.9.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.9.0
[0.8.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.8.0
[0.7.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.7.0
[0.6.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.6.0
[0.5.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.5.0
[0.4.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.4.0
[0.3.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.3.0
[0.2.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.2.0
[0.1.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.1.0
