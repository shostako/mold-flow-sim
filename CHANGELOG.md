# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

`0.x` 系のため、マイナー版の更新に後方非互換の変更を含むことがある。
バージョン 0.1.0〜0.14.0 は**開発履歴から遡及的に付与**したもの（2026-08-07 時点で整理）。

## [0.32.0] — 2026-08-22

**二相ショートショットのアニメーションが充填先端と同じスクラバ（再生 / コマ送り /
シーク / 速度）で見られるようになり、計量体積の既定が現在の形状のキャビティ体積に
なった。**

### 追加

- `core.visualizer.export_two_phase_frames` / `two_phase_frame_labels`。`frame_states`
  を単一ソースに GIF・フレーム PNG・プレイヤーの読み出しが同じコマ系列に乗る。
  読み出しは射出相が `t = … s`、圧縮相が `前進 … %`（モデルは圧縮の時計を持たない）。
- `build_fill_player_html(labels=...)`。1 コマ 1 文字列の読み出し差し替え。
- 結果 ZIP に `two_phase_player.html`（単体 HTML、オフラインで完結）。

### 変更

- 計量体積 `V_shot` の既定値が固定 5.0 cm³ から**現在の形状の最終キャビティ体積**
  （完全充填ちょうど）になった。形状を変えると追従し、ユーザーが値を触った後は
  触らない（`mfs_shot_volume_auto` に前回の自動値を持って比較。値は丸めない —
  丸めた既定は solver 側の素の体積比較で極小ショートショットになる）。このため
  `build_geometry()` はメインカラムでなくサイドバー内で呼ぶ。ショートショット欄は
  `st.container()` のプレースホルダで位置を確保し、中身は「出力」欄と版表示の後に
  埋める — 不整合時の `st.stop()` で版表示が消えないようにするため。
- 計量体積欄のキャプションが「前回実行の形状」でなく現在の形状の体積を出す。
## [0.31.1] — 2026-08-22

**Profile gate の「スペック入力」ラジオを撤去。ローカル一覧 + ドロップの 1 経路だけになった。**

### 削除

- `デモプリセット` と `JSON貼り付け` の選択肢、`SpecMode` enum、`SpecOrigin.DEMO` /
  `SpecOrigin.PASTE`。図面から起こしたスペックはファイルとして置く運用が定着し、
  貼り付けで読ませる場面が無かった。デモ形状は Film gate 1〜3 / Direct gate の既定入力が
  既に担っているので、Profile gate 側に同梱デモを選ばせる意味も無くなった
  （`data/gate_profiles/demo_profile_gate.json` 自体はテストと CLI が使うので残る）。
  `choose_spec_origin` は `mode` 引数を失い `has_upload` / `has_local` だけを取る。

## [0.31.0] — 2026-08-21

**ウェルドラインが「2 つの流れが出会う角度」で描かれるようになり、穴や肉盗みの後ろに
伸びる合流線が見えるようになった。濃い赤＝ウェルド（開き角 45° 以上）、薄い赤＝メルド
（ほぼ平行に合流する痕）。**

### 変更

- ウェルド検出器を全面差し替え。旧来の「8 近傍のうち 6 個以上が自分より早く充填」は
  実質的に局所最大の検出で、合流した後も流れ続ける線（穴の後ろ、肉盗みの下流）を
  構造的に見落としていた — 下流側 3 セルが必ず遅いので最大 5 個しか揃わない。新しい
  検出器は各セルで向かい合う隣接ペア（x / y / 対角 2 本）を見て、両側の流れが**そのセル
  へ向かい**、かつ両側より**遅く充填**（到着時刻の稜線）なら合流点とし、2 つの流れ方向の
  なす角（180° = 正面衝突、0° = 平行）を強度にする。商用 CAE の「合流角 135°」境界
  （開き角 45°）でウェルドとメルドを分ける
- `FlowResult.weld_angle_deg`（合流の開き角 [deg]、合流なしは NaN）を追加。`weld_score`
  はこれを既定しきい値で写した値。描画側が角度場を再しきい値化するので、しきい値を
  変えるのに解き直しは要らない
- 等値線・ウェルド図の凡例に weld / meld の 2 階調を追加。メルドも最低 35% の不透明度で
  描く（角度が小さくても「線がある」ことは見える）
- サイドバー「出力」に「メルド表示の下限角 [deg]」スライダー（0〜40、既定 0）。
  settings.json の `output.weld_min_angle_deg` に記録。解析後にスライダーを動かすと、
  キャッシュした結果から weld.png だけ描き直し、ZIP 内の画像と settings.json も差し替える
  （Codex P2: 実行ボタンの外で動かしても次回実行まで無反応だった）
- 誤検出の抑制 3 点: (1) 壁際セル（8 近傍に壁を含む）の流れ方向は片側差分で階段状に
  ぶれるので合流判定に使わない、(2) ゲート周囲 2 セルは除外、(3) 両側への到着時刻の
  落差が 10:1 より偏るセル（障害物の角を曲がる流れが片側の等時線に乗っただけ）は稜線と
  認めない。対称形状で中央 2 列が機械精度で同値になる場合は、同値側を 1 セル先で読む

### 既知のトレードオフ

- 穴の後ろの合流線は、根元（最初に触れる数 mm）だけが 45° 以上のウェルドで、残りは
  開き角数度のメルドとして上端まで続く。これは薄板が厚いランプから線状ソースとして
  養われ、ノッチが横流れで埋まっていく模型の挙動そのもので、等値線の中央の「舌」と
  対応する。既定の下限角 0 でメルドまで全部描き、ノイズが気になる図面ではスライダーで
  上げる

## [0.30.0] — 2026-08-21

**Film gate が 3 系統になり、肉盗みの水平部（溶接ダム）の PL からの距離をスライダーで
振れるようになった。0 にすると鋼材が PL に接して完全な肉抜き空洞（穴）になる。**

### 追加
- `Film gate 2 (扇状/肉盗み2)`: `hamoko_gate_furiwake_weld_20260818` を既定とする対称版。
  肉盗みの下流 t=7〜17 が溶接で水平（`island.weld`、残り流路厚 0.1）、外壁開始 t=5、
  井戸壁角 71.6°（床範囲 = `深さ/tan(71.6°) = 1.5` ずつ内側）。図面の肉盗み境界線は
  t=0 始まりだが UI は t=ランド長 に留めるので、同じ直線を t=1 で読んだ 47.64 を既定に置く
  （1.0mm 格子の結果は spec 直読みと bit 一致）
- 肉盗みを有効化した全 Film gate に「水平部（溶接ダム）」チェックボックス。開始 t と
  「PL からの距離（残り流路厚）」の 2 スライダー、終端は肉盗み終端に固定。既定 ON は
  Film gate 2 だけ
- `GateProfileSpec.validate()` が `island.weld.depth = 0` を受理し、ビルダーはその帯を
  mask から外す（厚み 0 のセルを cavity に残すと `S = 0` で特異系になる）。井戸が貫く
  セルは井戸の深さで cavity に残る

### 変更
- ラベル: `Film gate 1 (肉厚調整ゲート)` → `Film gate 1 (扇状/肉盗み1)`、旧
  `Film gate 2 (肉厚調整ゲート・片側)` → `Film gate 3 (片側/二倍流動長)`（widget key も
  `f2_` → `f3_`、記録名 `film_gate_3_parametric`）。UI 既定入力は引き続き片側版
- 用語: UI の「アイランド」を「肉盗み」に、「バルブオリフィス径」を「バルブゲート径」に
  統一。JSON キー（`island` / `orifice_diameter`）とコード識別子はそのまま — 既存スペックは
  無変更で読める
- 井戸の壁角は `_WELL_WALL_ANGLE_DEG` 定数から `_ProfileGateDefaults.well_wall_angle_deg`
  に移り、図面ごとに持つ（1 / 3 = 60°、2 = 71.6°）。外壁開始 t も既定値側に移動

## [0.29.0] — 2026-08-21

**Film gate 2 も肉厚調整ゲートのパラメトリック入力になり（片側版）、UI 既定が
二相ショートショット運転に合わせて切り替わった。**

### 変更

- **Film gate 2 を差し替え** — 旧「ゲート位置可変（直角台形ランナー）」モデルは UI から外し、
  Film gate 1 と同じ深さ場モデルの **片側版**（`symmetric=False`、バルブは w=0 端、幅は
  バルブ側端から測る）にした。ラジオの表記は `Film gate 2 (肉厚調整ゲート・片側)`。
  既定値は `hamoko_gate_2bai_20260703`（出口幅 299 / アイランド境界幅 95.3→20.0 /
  外壁 (3,299)→(23.6,4.45) / バルブ t20.0 Φ3、ランド・ランプ・井戸は Film gate 1 と同じ）。
  既定のまま実行した形状は spec を Profile gate で読んだ結果と mask・肉厚・ゲートセルが
  一致する（`tests/test_film_gate2_ui.py`）
- 導出量の結び付きは Film gate 1 と同じだが2点違う：外壁開始の幅 = **出口幅そのもの**
  （片側は半幅でない）、バルブ位置の既定 = 図面の値 20.0（井戸中央 21.5 ではない）
- Film gate 1 / 2 のサイドバーと組立を共通ヘルパー（`_profile_gate_sidebar` /
  `_profile_gate_from_inputs` / `_build_film_gate`）に統合。widget key は `f1_` / `f2_` で分離
- **UI 既定値**: 壁面冷却モデル『なし』（旧: 層別）、射出圧縮成形 ON・ストローク 0.50 mm
  （旧: OFF / 0.70）、二相ショートショット ON（旧: OFF）。二相モデルが実際に走る組合せを
  初期状態にした
- 設計図キャプションの原点表記を「バルブゲート位置（赤丸）」に（片側では製品中央でない）
- 旧 `FilmGate2Config` / `build_film_gate2_geometry` と 33 テストは CLI / ライブラリ用に残置
- Film gate の全 widget に `f1_` / `f2_` 付き key（key 無しだと同ラベルの値が入力切替で引き継がれる）。
  バルブの事後ガードは中心セルでなく**オリフィス円と mask の交差**で判定（片側は中心が w=0
  境界上にあり、細メッシュで誤拒否していた）（Codex P2 ×2）

## [0.28.0] — 2026-08-21

**UI の Film gate 1 が、実機の肉厚調整ゲート（ランド／メインランプ／アイランド／井戸／外壁線）
をスライダーで組むパラメトリック入力になった。**

### 変更

- **Film gate 1 を差し替え** — 旧「台形＋半円ランナー＋▽肉盗み」モデルは UI から外し、
  Profile gate と同じ深さ場モデル（`GateProfileSpec`）をサイドバーで組み立てて
  `build_profile_gate_geometry` に流す形にした。ソルバ・可視化は無変更。ラジオの表記は
  `Film gate 1 (肉厚調整ゲート)`
- 既定値は `hamoko_gate_furiwake_20260703` の寸法（出口幅 298 / ランド 0.35×1.0 /
  ランプ 10.95°・cap 2.5 / アイランド 2.5°・境界半幅 52.7→10.0・終端 17 /
  外壁 (3,149)→(23.3,4.5) / 井戸 t15.5–27.5・半幅 4.5・深 4.5 / バルブ t21.5 Φ3）。
  既定のまま実行した形状は、その spec を Profile gate で読んだ結果と mask・肉厚が
  bit 一致する（`tests/test_film_gate1_ui.py`）
- スライダーに出すのは主要寸法のみ。導出量は図面の結び付きどおり固定：アイランド境界線の
  t 端点 = ランド長 / アイランド終端、外壁開始の半幅 = 出口幅/2、井戸の床範囲 =
  t_range を `深さ/tan(60°)` ずつ内側に寄せた範囲（壁角 60° は定数）、バルブ位置の既定 =
  井戸中央。アイランド・井戸はチェックボックスで OFF にできる
- UI が先回りする制約: 井戸深さ ≤ 半幅·tan(60°)（壁が届かない深さは spec が受理するのに
  浅く描かれる）、バルブ位置 ∈ [半径, ポケット終端 − 半径]（外れるとビルダーが最寄りセルへ
  黙ってスナップし、記録と注入位置がずれる）。幅方向の外れはビルド後に検出して拒否（Codex P1 ×2）
- `GateProfileSpec.validate()` に井戸深さの到達可能性チェックを追加（`depth ≤ half_width·tan(wall_angle)`、
  壁角 90° は無制限）。JSON 経路にも効く — 旧来は受理して浅く描いていた
- 旧 Film gate 1 の `FilmGateConfig` / `build_film_gate_geometry` と CLI の
  `FILM_GATE_CASES`、43 テストはそのまま残る（CLI 専用）

## [0.27.0] — 2026-08-20

**二相ショートショットに履歴アニメーションが付き、設定の干渉が見えるようになった。**

### 追加

- `render_two_phase_animation()` — 二相履歴の GIF。射出相は実時間（到着時刻）で青が育ち、
  圧縮相は正規化順序で橙が前進する（モデルは圧縮の時間スケールを持たない — タイトルに
  どちらの時計かを明記）。フレーム系列は純データの `frame_states()`（`core/two_phase.py`）
  が供給し、充填集合の単調成長と最終フレーム = Ω₂ を保証。UI の expander と ZIP
  （`two_phase.gif`）、CLI 出力に同梱
- UI: 計量体積入力の下に前回実行形状のキャビティ体積目安（最終 / 開きギャップ）を表示

### 修正

- **凡例を図の外（下部中央）へ** — 横長プレートでは axes 内のどのコーナーも製品に被る
  （実レンダで右上コーナーが隠れた）。figure レベルの凡例はアスペクト比によらず衝突しない。
  タイトル設定後にレイアウト確定（先に tight_layout するとタイトルが上端で切れる）
- **壁面冷却モデルとの干渉を常時可視化** — UI 既定の壁面冷却は『層別』なので、既定のまま
  二相を ON にすると実行時警告一発だけでスキップされ「チェックしても何も起きない」ように
  見えた。サイドバー（設定と同じ場所）に常時警告を出し、スキップ理由を session_state に
  持たせて結果ペインにも残す

## [0.26.0] — 2026-08-19

**計量制限のショートショットを実機パラメータのまま予測できる二相モデルを追加。**

### 追加

- `core/two_phase.py` — 二相ショートショットモデル `solve_two_phase_short_shot()`。
  (1) **射出相**: 型開きギャップ（h + stroke）で τ を解き、体積CDFを計量体積
  V_shot で切って射出終了時の溶融プール Ω₁ を得る。(2) **圧縮相**: 最終肉厚で
  τ を再解（Ω₁ 全セルを等圧ソース Dirichlet に）、体積保存まで τ 順に前進させて
  圧縮後の最終形状 Ω₂ を得る。線形求解2回・時間積分なし。同値 τ のタイ群は
  原子的に扱う（半セル充填は作らない）。ICM OFF / stroke 0 では Ω₂ = Ω₁ に退化
  （純粋な体積律速ショートショット）
- `render_two_phase_map()` — カテゴリカルマップ（青=射出で充填＋射出等時線、
  橙=圧縮で前進、灰=未充填）
- UI: サイドバー「ショートショット（計量制限）」expander（計量体積 [cm³] 入力）、
  結果ペインの専用 expander（マップ + 充填率メトリクス）、ZIP に
  `two_phase_short_shot.png` / `two_phase_metadata.json` を同梱、settings.json に記録
- CLI: `run_demo.py` に `two_phase_shot_volume_cm3` kwarg と参照ケース
  `FilmGate_PP_two_phase_short`（段差プレート + stroke 0.70 + 計量 4.5 cm³）
- テスト 24 件（解析解のストリップ検証・体積保存・タイ原子性・等圧ソース契約・
  ネスト単調性・AppTest 配線 3 件）。変異注入 8 件を複製リポで全検出

### 意図した制約（バグではない）

- 圧縮中の凍結進行は無視（スキン層モデルとの併用は入口で拒否）。計量律速の
  ショートショットは凍結の物理を含まないという用途定義そのもの
- 射出/圧縮のオーバーラップ運転は表現外（厳密に逐次の二相）
- 溶融プールは等圧ソース近似（プール内圧損 ≪ 未充填先端の抵抗、薄板で妥当）

---

## [0.25.0] — 2026-08-19

**充填時刻の写像を物理に合わせ、時間スケールを1セルに支配されない正規化に変えた（Issue #52）。**

### 変更

- **到達時間の写像を体積CDFに**（`HeleShawSolver` / `MultilayerHeleShawSolver` 共通）:
  セルの充填時刻 = そのセル以下の τ を持つセル群の体積 / Q。定率射出で先端は体積線形に
  進むので、これが物理そのもの。旧 `(τ/τ_max)·T_fill` 線形写像は健全な1Dストリップでも
  中央セルを 0.75T と誤報していた（正しくは 0.5T）。外れ値1セルは自分の体積分しか他セルの
  時刻を動かせず、外れ値を1個足しても既存セルの絶対時刻は厳密に不変。
- **T_fill 膨張プロキシを体積重み付き平均 τ の比に**: 分子・分母とも同一の still-flowing
  集合上で評価。旧実装は frozen 除外 max ÷ 全セル max で、分母が病的1セルに支配され、
  そのセルが凍ると分子からだけ抜けて比が雪崩れる構造だった。実測値は metadata の
  `tau_rep_flow` / `tau_rep_baseline` に出す。
- **スキン層の不動点を live 領域ごと解き直す**（`_solve_domain` + 外側ドメインループ）:
  生きた領域のスキン・充填時間が、樹脂の届かない死領域の体積に依存しない。合成ケースで
  live 結果は死領域の量（20/100セル）に対しビット単位で不変。
- **`skin_max_iterations` 既定 5 → 20**: 定圧近似の正帰還（スキン成長→抵抗増→T_fill増→
  さらに成長）は臨界を越えると設計上雪崩れる。旧既定は雪崩の途中で上限に当たり、
  凍結ゼロ・膨張27倍のもっともらしい中間状態を `skin_converged=False` の旗だけ立てて
  返していた。
- 既知のトレードオフ: **スキン成長の時計は到着時刻のまま**（到着スナップショット意味論）。
  正しい到着時刻に直した結果、ゲート脇の薄肉チョークは「自分の体積/Q ≈ 0 秒」でスナップ
  ショットされ、ほぼ凍結しなくなった。物理的にはゲート近傍こそ充填の間ずっと老化し続ける
  （露光時計 s(T_fill − t_arr) が正しい向き）。旧線形写像はこれを偶然保守側に誤魔化して
  いた。別 issue で追跡。
- **ドメインループの安全弁を graceful に**（Codex P2）: `MAX_DOMAIN_PASSES`（4→64、
  領域は毎パス厳密に縮むので本来は不要な暴走止め）に当たって残った凍結セルは、
  解き直しなしで unfillable 側に落とし、`metadata["domain_converged"]=False` を立てる。
  旧実装は残った凍結セルが live のまま有限の充填時刻を持ち、`short_shot_cells` が
  `unfillable_cells` を超え得た。

## [0.24.1] — 2026-08-19

**ゲートに繋がらないキャビティ領域を、もっともらしいゴミで埋める代わりに入口で拒否する
ようにした（Issue #58 修正）。**

### 修正

- **`solve()` がゲート未到達成分を検出して `ValueError` を出す。** どのゲートからも
  切り離された連結成分は Dirichlet 点を持たない純 Neumann ブロック＝特異行列で、
  `spsolve` は警告なしにゴミを返す。切り離され方によってゴミは天文学的な τ（画面で
  気づける）にも、**領域全体が同時刻に充填されたかのような一様な充填時間**（画面に
  手掛かりが無い）にも化ける。後者が Profile gate の既定メッシュで実際に起きていた。
  検出は `check_gate_reachability()`（`core/solver.py` のモジュール関数）で、5点
  ステンシルと同じ **4近傍**の連結成分ラベリング。8近傍だと対角接触だけの「橋」を
  連結と誤認して特異ブロックを見逃すため、連結性の定義自体をテストで固定した
- **層別ソルバーにも同じ検査。** `MultilayerHeleShawSolver.solve()` は base の
  `solve()` を経由せず `_solve_tau_field` を直接叩くので、base 側だけの検査では
  層別経路が素通りする
- **Profile gate は組立段階で先回りして拒否。** `gate_exit_width` がメッシュ間隔を
  切ってゲート土手が最下行を全閉するとき（例: 出口幅 0.5 mm / セル 1.0 mm）、
  ソルバーまで行かずに「出口幅を広げるかメッシュを細かくせよ」とパラメータ名入りで
  拒否する。ソルバー側の検査は任意 mask への安全網としてそのまま残る
- **`unfillable_mask` に落とさず拒否を選んだ理由**: 切断は形状の作り間違いであって
  「物理的に樹脂が届かない」ではない。未充填機構に流すと「入力が間違っている」が
  「モデルがショートショットを予測した」に化ける
- ゲートは在るが全て mask 外、というケースも「no gate lies inside the cavity mask」で
  拒否（従来は空ゲートのみ検査）

### テスト

- 422 → **427**（+5）。strict xfail で張っていた境界テストが修正で XPASS に転じたので
  マーカーを外し、「拒否される」ことを現在形で assert する形に書き換えた。
  変異注入 5 件（base 検査削除 / 層別検査削除 / 土手検査削除 / 8近傍化 / 判定反転）
  すべて、狙ったテストが 1 対 1 で検出することを複製リポ上で確認済み

---

## [0.24.0] — 2026-08-19

**用語を「ショートショット」に統一し、`README.md` の古い記述と、実装と食い違っていた
方程式の符号を直した。**

### 変更

- **「短ショット」→「ショートショット」**（20 箇所）。日本の成形現場で使うのは
  short shot の音写である**ショートショット**で、「短ショット」は半端に訳した造語。
  しかも既に `ショートショット` も 8 箇所で使われていて**両表記が混在**しており、
  `README.md` では同じ節の中でスキン層を「ショートショット予測」、層別を
  「短ショット判定」と書き分けていた。コード側の識別子（`short_shot_mask` 等）は英語なので不変。
  `logs/` は日付付きの作業記録なので触っていない
- **方程式の符号を実装に合わせた。** `README.md`（2 箇所）と `core/solver.py` の
  docstring が `∇·(S∇τ) = 1` と書いていたが、実際に組み立てているのは対角 `+Σcoeff` /
  非対角 `−coeff` / 右辺 `+1`、すなわち **`−∇·(S∇τ) = 1`**。
  結果の τ は正しかったが、読む側が符号を追うたびに引っかかる。`CLAUDE.md` は
  この食い違いを注記していたが、注記するより直す方がいいので直した
- **`README.md` のロードマップを現状に合わせた。** 1〜3 は実装済み（基盤整備 /
  パラメトリックゲート 3 種 + JSON スペック / スキン層・層別・剪断発熱 段階1）なのに
  未着手のように読めた。済みと残りを分け、残りには剪断発熱 段階2 を追加
- **「局所粘度反復」の記述が層別ソルバーを取り違えていた**（Codex 指摘）。
  「代表剪断速度で Cross-WLF を 1 回評価するだけ」は壁面冷却なしモードの話で、
  層別モードは反復ごとに `(N, ny, nx)` の粘度場を解き直している。欠けているのは
  γ̇ の由来 — Poiseuille 分布に渡す速度が射出速度の単一代表値 `V` に固定で、
  解いた τ から局所流速に戻す環が無い。ロードマップ項目と機能表の行名を
  「流動場から速度・剪断速度へのフィードバック」に改めた
- **`A` を「正定値形」と書いていたのを取り消した**（Codex 指摘）。この符号の取り方で
  制約前の作用素は対称半正定値だが、`_build_linear_system` は Dirichlet を**行にしか**
  適用しない — ゲート行を単位行に潰す一方、隣接する内部行はゲート列の `−coeff` を
  残す。したがって組み上がった `A` は非対称で SPD ではない。この記述を根拠に
  ロードマップの CG / AMG を対称専用ソルバーで実装すると収束保証が消える。
  `core/solver.py` の docstring・`README.md`・`CLAUDE.md` の 3 箇所を訂正し、
  ゲート列の消去が前提であること（ゲートで `τ = 0` なので消去は厳密）を明記した。
  この 2 つの主張はコードについての主張なので `tests/test_solver_1d.py` に回帰テストを
  足した（非対称エントリがゲート行に限られること／消去が解を動かさないこと）。
  どちらも「対称化する」修正を入れた後もそのまま通る。
- **顧客向け技術資料の SPD 保証も訂正した**（Codex 指摘）。
  `docs/流動解析の仕組み_想定問答_技術編.md` が「本ツールの方程式は対称正定値」と
  顧客に断言していた。**結論（解の存在と一意性）は生きている** — ゲートで値が固定
  される未知数を消去した内部ブロックは実測で対称かつ最小固有値 1.3e-07 > 0、
  内部系だけを解いた結果はフル系の解と 5.7e-15 で一致する。誤っていたのは根拠の
  書き方だけなので、保証を取り下げるのではなく「固定点を消去した本体が対称正定値」
  に正確化し、実装が行にしか書き込まない簡便法である旨を併記した。
  この性質は顧客への約束なので `tests/test_solver_1d.py` に検証を足した。
  **この見落としの原因は走査を怠ったこと** — SPD の訂正時に `grep` の対象へ
  ファイル名を並べてしまい、リポ全体を走査していなかったので `docs/` が丸ごと
  視界の外にあった。改めて全体を走査し、`docs/` に用語（短ショット / short shot）
  と符号の取りこぼしが無いことも確認した
- **「線形だから一回で解ける」の節を既定モードに合わせて書き直した**（Codex 指摘）。
  技術編は「反復計算なしに一回で厳密な答えが出る」「収束するかどうかの不確実性が
  そもそも生じない」と書いていたが、これは壁面冷却モデル「なし」の話。**UI の既定は
  層別**（`app.py` の `wall_model` ラジオが `index=2`）で、スキン層・層別はどちらも
  τ ↔ T_k ↔ η_k を fixed-point で反復する。既定の挙動について顧客に逆のことを
  言っていた。3 モードを分けて書き直し、反復する 2 モードでは一般論として収束が
  保証されないこと、その代わり反復回数と収束可否を毎回表示し damping で安定化して
  いること、取り戻している非線形が温度経由のみであることを明記した
- **SPD 保証にゲート到達条件を明記した**（Codex 指摘）。どのゲートからも切り離された
  キャビティ成分があると、そのブロックは純 Neumann ラプラシアンで固有値ゼロが残り、
  解が一意に定まらない。実測で最小固有値 9.8e-20 / 条件数 1.6e+16、しかも
  `solve()` は例外を出さず完走して τ = 8.85e+20（正常値の 4.2e14 倍）を返す。
  文書側は条件を明記して修正。**実装側の頑健化は Issue #58 に分けた** —
  標準ビルダーはいずれも連結な mask を作るので UI からは到達しないが、
  `Geometry` は任意の mask を受け付ける公開 API なので将来踏みうる。
  境界は `tests/test_solver_1d.py` が「拒否されるか、さもなくば特異」の形で押さえて
  あるので、Issue #58 をどちらの案で直してもテストは通る
- **上記の続き（Codex 指摘4件）。** (1) 連結条件の「標準ビルダーはいずれも繋がった形を
  作るので通常の使い方では外れない」は**誤り**だった。Profile gate はゲート出口幅が
  メッシュ間隔より細いと `core/profile_gate.py:757-763` の gate land wall が出口行を
  全部閉じ、製品全体がゲートから切り離される。**既定の 1.0 mm/cell でも起きる**
  （実測: `gate_exit_width=0.5` で孤立セル 14700 = プレート全体）。しかも end-to-end では
  例外も出ず、製品全体が `0.5241 s` の**一様値**になる — 桁外れの数字と違い画面から
  気づけない。Issue #58 の重要度を上げて追記した。(2) 「この2モードは各層温度から粘度を
  引き直し damping し収束を表示する」は**スキン層に当てはまらない**。スキン層は
  `eta` をループ前に 1 回決めて固定し、反復するのは `h_core` だけ。damping は
  `multilayer_solver.py` にのみ 11 箇所、収束表示も `app.py` の `if multilayer_on` の中だけ。
  2 モードを分けて書き直した。(3) 回帰テストが `unfillable_mask` 案での修正を許容して
  いなかった。数学の主張（ブロックが特異）と実装への要求（充填時間を捏造しない）を
  2 本に分け、後者を `xfail(strict=True)` にした。(4) 他の顧客向け資料が「粘度を 1 回
  評価して 1 回解く」と断言したままで、この PR が書き直した技術編と矛盾していた。
  `想定問答.md` 2 箇所・`プログラム解説.md` 3 箇所を「なし」モードに限定して書き直し、
  反復するモードの説明を足した。技術編のレベル表も B を「がんばれば届く」から
  「実装済み、ただし精度保証はできず粘度反復は温度経由のみ」に更新
- **`顧客向け解説.md` の末尾もユーザー判断のうえ実態に合わせた。** 「選択肢が並んでいても
  中身の計算が伴っているわけではありません」「参考程度にもならない状況です」
  「プログラムの改造は停滞中です」と書いていたが、層別ソルバーは 42 テスト付きで
  実装済みかつ UI の既定モードで、事実と食い違っていた。**限界の表明は落とさず**、
  「実装済みで動くが精度は保証できない — 物理モデルが大きく簡略で、何より実機との
  照合による較正を行っていない」という書き分けにした。オプションの位置づけを
  「固化を考慮すると傾向がどちらへ動くかを見るもの」と明示し、ツール全体の位置づけ
  （絶対値でなく相対比較）と同じ性格であることを添えた。✅❌ 表の「流動中の局所的な
  粘度更新（代表値で1回評価）」も「流速から粘度へのフィードバック」に改めた
- **上の書き直しが生んだ資料内の不整合を潰した**（Codex 指摘4件）。修正が新しい
  食い違いを作る典型で、うち1件は**技術編で直したばかりの誤りをプログラム解説で
  作り直していた**（「反復回数と収束の可否を画面に表示」を両モードについて書いた。
  表示は `app.py` の層別ペインだけ）。名指しで直すのをやめ、(A) 一回で解く系の断言
  (B) 反復・収束の表示 (C) スキン層だけが冷却対応と読める箇所 の 3 クラスで
  `docs/` 全体を走査してから直した。`商用CAE_物理モデルと数式.md` の「一発で解く」
  2 箇所は**時間発展しないという意味なので今も正しく**、区別して残した（層別の
  fixed-point は時間発展ではない）。あわせて `xfail` に `raises=AssertionError` を
  指定 — 無指定だと無関係な `RuntimeError` まで「予想どおりの失敗」として飲み込み、
  回帰が XFAIL に化けて見えなくなる（変異注入で確認）
- **層別モデルを「3次元」と分類していたのを訂正**（Codex 指摘）。レベル表で B
  「簡易な3次元層別」を実装済みにしていたが、層別ソルバーは**厚み方向を積分して
  2次元の流動性に戻す**モデルで、面内の未知数は `τ(x,y)` の 2 次元のまま。同じ資料の
  「本ツールは A に徹し」とも食い違っていた。A を「2次元＋厚み方向だけ擬似3次元」、
  B を「面内も含めた簡易3次元」に組み直した。あわせて「取り戻している非線形は
  温度経由のみ」も訂正 — 温度経由は層別の話で、スキン層が繋いでいるのは流路の
  狭まりを経由した環
- **反復の記述を実装の細部に合わせた**（Codex 指摘3件）。(1) スキン層の `s` に渡して
  いるのは局所流速ではなく `τ` を正規化した到達時刻 `t_arr`（`core/solver.py:405-425`）。
  「流れが遅い所ほど固化層が厚く」は、直後の「流速からの環は繋いでいない」と紛らわしい
  うえ実装と違う。「到達が遅いセルほど、そこに至るまでに冷える時間が長いぶん」に改めた。
  (2) 「どちらも釣り合うまで繰り返します」は常に収束するように読める。実際は
  `skin_max_iterations` / `max_iterations` に達したら未収束のまま打ち切るので、
  技術編の「一般論として収束は保証されません」とも矛盾していた。打ち切る旨を明記。
  (3) 層別の反復の戻り先。粘度を引き直すと流動性 `S` も行列も組み直すので
  （`multilayer_solver.py:483-489`）、「5 以降と往復」ではなく 3〜5 を経て 2 に戻る
- **零固有値の検証が符号に依存していた**（Codex 指摘）。`ev.min() / abs(ev).max() < 1e-12`
  は、内部ブロックが**負定値になったときに素通りする** — 零モードが1つも無くても比が
  負になるので常に通り、テスト名が主張する「解が一意でない」を検証できていなかった。
  今セッションで自分が別途見つけた「空虚なガード」と同じ型。半正定値性
  （`ev.min()/scale > -1e-12`）と零モードの存在（`abs(ev).min()/scale < 1e-12`）を
  別々に assert する形に直した（負定値注入で検出を確認）。あわせて、スキン層に3つ目の
  打ち切り条件（ゲート以外の流路が全部塞がったら反復終了、`core/solver.py:461-466`）が
  あることを資料に追記 — 計算の失敗ではなくショートショットという結果であることも添えた
- **日本語の文中に残っていた英語表記 `short shot` も統一した**（Codex 指摘）。
  `app.py` の 3 箇所（固化しきい値のヘルプ、スキン層 expander のタイトル、
  コア層のキャプション）と `README.md` 1 箇所、`CLAUDE.md` 3 箇所。
  matplotlib の図中ラベル・英語の docstring / コメント・テストの説明文は
  英語の文脈なので据え置き。`CLAUDE.md:97` の見出し `**short shot**` も後から
  追加で直した — 最初の走査が識別子を含む**行ごと**除外していて、`short_shot_mask`
  と散文が同居する行が丸ごと落ちていた。識別子を文字列として潰してから残りを
  見る形に走査を組み直した結果、他に散文の取りこぼしは無いことを確認済み
  （残る英語表記は図中に描かれる文字列の引用・語源の説明・英語の文脈のみ）

### `README.md` で確認して問題なかったもの

- `--cases PP_baseline PP_dual_gate` — 両方とも `run_demo.py` に実在する
- 材料リスト 8 種、Python 3.11+、デプロイ設定の但し書き — 現状と一致

---

## [0.23.0] — 2026-08-19

**画像入力（PNG/JPG の二値化から形状を起こす経路）を撤去した。**

一度も使われていない。二値化しきい値と 1px=1mm 仮定で外形を起こす方式なので精度の裏付けも無く、
図面 PDF から JSON スペックを起こして読み込む経路（Profile gate）が確立した今、担う役目が無い。
入力ラジオから隠すだけでなく **`core.geometry_from_image` ごと消した** — どの入口からも到達
できない関数は、誰も検証しない能力の主張として腐るからだ。戻すなら git 履歴から拾う。

### 削除

- 入力ラジオの `画像から生成 (PNG/JPG)` と、そのサイドバー入力群
  （画像アップロード / 均一肉厚 / px→mm 換算 / 反転 / 二値化しきい値）
- `build_geometry()` の画像分岐。アップロード画像を `tempfile` に書き出す処理も併せて消えた
- `core.geometry_from_image` と `core/__init__.py` の export、`core/geometry.py` の `PIL` import

### 変更

- `build_geometry()` の末尾は `AssertionError` を送出する。全分岐が return するので、
  ここに到達するのは**入力ラジオに選択肢を足してビルダーを足し忘れたとき**だけ
- `config_settings()` の `cfg` を**必須**にした（後述）。`cfg=None` を渡すと `TypeError`
- 適用範囲の但し書き、`README.md` の形状入力、`CLAUDE.md` の該当記述を更新

Pillow 依存は**残る**。GIF 書き出し（`PillowWriter`）とプレイヤーのフレーム寸法取得が使う。

`assets/`（画像入力用の置き場として `CLAUDE.md` に載っていた）はそもそも存在しなかったので
記述ごと削除した。

### 消し残しの追跡（Codex レビュー3周）

削除は「消したもの」より**消し残し**で失敗する。3周で出た指摘と、その原因。

| 周 | 見つかったもの | 原因 |
|----|--------------|------|
| 1 | `README.md` の対応表 / `.streamlit/config.toml` の上限の根拠 / `PROGRESS.md` の未完了タスク | 走査語が `画像から生成` と `geometry_from_image` だけで狭かった |
| 1 | `config_settings(cfg=None)` が到達不能に | 画像入力が唯一の利用者だったことを見落とした |
| 2 | `core/solver.py` / `tests/test_fill_render.py` / `core/visualizer.py` / この節の自己矛盾 | 走査語は広げたが、**154 行の結果を `head -25` で切って**「残骸なし」と判断した |
| 3 | `CLAUDE.md` の結果 ZIP の契約（「入力画像も名前とハッシュだけ記録する」） | 走査語が `画像入力` で、実際の表記は**語順が逆の「入力画像」**だった |
| 3 | この節に残った重複ブロック | 節の置換で終端を `---` で探したが、**Markdown の表の区切り行 `\|------\|` に `---` が含まれ**、最初の表の途中で切れた |

得られたもの:

- **`head` で切った走査は、走査しないより悪い。** 25 行見て全件見た顔をすると偽の確信が残る。
  切るなら落とした件数を言う
- **日本語の複合語は語順で引っかからない。** 「画像入力」で引いて「入力画像」を逃した
- **`---` を節の区切りとして探すな。** 表がある文書では区切り行に含まれる。行頭・行末を含めて
  アンカーする
- **走査は候補を出すだけで、判断は文脈を読んでから。** `app.py` の「画像から寸法を測って
  逆算する羽目になる」は誤検出で、この画像は ZIP に入る出力図のこと。機械的に直せば壊していた
- **文書は継ぎ足すな、節ごと書き直せ。** この節の自己矛盾（`cfg=None` の契約を前半と後半で
  逆に案内）は、修正の前後で別々に書いた結果だった

`config_settings()` の `cfg` は**必須**になった。`cfg=None` を渡すと `TypeError`。
`config` キーを持たない記録の形は、もう何も生成せず、したがって何も検証しない。
空 source のテストは有効な config を渡す形に直した——cfg 必須化により、そのままだと
**落ちた理由が source か cfg か判別できなくなる**ため。

---

## [0.22.0] — 2026-08-19

**ローカルに置いたゲートスペックを、毎回アップロードせずに一覧から選べるようになった。**

図面から起こしたスペック JSON はこのリポ（public）に置けないので、これまでは比較のたびに
ファイルダイアログでローカルフォルダまで潜ってアップロードし直していた。その手間だけを取る。
**保存するものは何も増えていない** — 置き場所を1つ指すだけで、コピーもキャッシュも作らない。

### 追加

- `core/spec_source.py` — スペックの所在解決と入力源の優先順位。IO を持たない純関数群
- 「スペック入力」ラジオの「JSONアップロード」を **「ローカルから読込」** に変更。
  ドロップ欄はそのまま残り、その上に一覧のドロップダウンが並ぶ
- ドロップと一覧の両方が生きているときは**ドロップが勝つ**（直近の明示的な行為）。
  勝った側をサイドバーに明示し、負けた一覧は `disabled` にする
- `tests/test_spec_source.py` — 44件

### 使い方

リポ直下に `local_specs` を作る（gitignore 済み）。一度だけ:

```
ln -s /path/to/specs local_specs
```

これが無ければ一覧は現れない。**それが公開インスタンスの既定状態**。

### 設計の理由

- **環境変数 `MFS_SPEC_DIR` を採らなかった。** フェイルオープンだからだ。Streamlit Cloud に
  環境変数の UI は無く secrets が唯一の口だが、`streamlit.runtime.secrets` はトップレベルの
  `str`/`int`/`float` を `os.environ` に昇格させる。ローカルの `secrets.toml` を Cloud の
  設定欄に貼る——secrets を移す普通の手順——だけで、公開インスタンスにファイル読み取り口が
  生える。gitignore されたパスは checkout + secrets で構成される Cloud 上に**存在させる手段が無い**
- **root を1つの gitignore 済みの名前に固定した。** 設定可能な root だと、リポ内を指して
  `git add -A` するのが顧客寸法を public リポに commit する最短経路になり、env ゲートも
  パス閉じ込めもこれには効かない。固定すれば構造的に起きない
- **一覧の既定は「— 未選択 —」。** ジオメトリは実行ボタンの後ろではなく毎 rerun 構築される
  ので、既定で実ファイルを選ぶと**ページを開いた瞬間に顧客形状が描画される**。加えて既定選択が
  あると「両方埋まっている」が常態化し、優先順位が毎回発火する
- **テキストのパス欄は作らなかった。** ユーザー入力のパスが無いので、`..`・`~` 展開・prefix 一致・
  拡張子縛り・大小文字の問題が**書き忘れではなく存在しない**。root 外の単発ファイルはドロップで読む

### 直したもの（実装中に自分のテストが釣った）

- `Path.resolve()` はシンボリックリンクループで `OSError` でなく **`RuntimeError`** を投げ、
  その文言に絶対パスが入る。`client.showErrorDetails` の既定は `full` なので、捕捉漏れは
  スペックフォルダのパスを画面に出す
- `Path.glob()` は**権限エラーを飲み込んで空を返す**。読めないフォルダが「スペック0件」と
  区別できない偽の緑になっていた。`os.scandir` に変更
- スペック読込経路が捕捉していたのは `JSONDecodeError` と `ValueError` だけで、
  `PermissionError` / `FileNotFoundError` は素通り。`OSError` と `UnicodeDecodeError` を追加し、
  **例外の文言は表示しない**（絶対パスが顧客名と案件名を含む）

### 意図したトレードオフ

一覧はフォルダ内の**全ファイル名を画面に出す**。アップロード方式では1件だった露出が
フォルダ内の件数になる。ファイル名は案件の識別子そのものなので、画面共有時の露出は増える。
利便性の直接の対価として受け入れる（expander は既定で畳んである）。

---

## [0.21.0] — 2026-08-19

### 配色ガードの根拠（Codexレビュー4周ぶん）

配色を差し替えたときに壊れないよう、テストは「名前が一致するか」でなく**実際に描かれる ramp の
測色**で判定する。レビューで4段階に鍛えられた:

| 指摘 | 実体 | 対応 |
|------|------|------|
| P1 stop のパース | Plotly は配色によって `rgb()` 形式と hex を返し分け `mcolors.to_rgb` が前者を拒否。Cividis はたまたま hex 側 | hex と `rgb()`/`rgba()` の両対応 |
| P2 ガンマ | ガンマ符号化のまま加重和を取っていた。`rgb(205,78,46)→rgb(242,34,36)` は加重和で減少・真の輝度で増加 | sRGB を線形化してから WCAG relative luminance |
| P2 補間 | ガンマ修正により「stop で単調なら全域で単調」の論拠が失効（Plotly は符号化 RGB で線形補間、輝度は非線形）。`bluered_r` が実際に stop 間で 0.0023 逆行 | stop 間を密サンプル |
| P2 可読性 | 単調なだけなら近黒→黒の ramp も通り、地図が真っ黒で読めない | 薄肉端と厚肉端の**コントラスト比 3:1 以上**（WCAG 1.4.11、`cividis_r` は 12.56:1）|

薄肉端の HSV 彩度ガードは残している。`cividis_r` の黄は白に対して輝度では **1.25:1** しかなく、
白背景から分離しているのは**彩度**であって明度ではないため — 明度で判定すると採用した配色自身を
弾いてしまう。彩度が「白と見分けられるか」、コントラスト比が「ramp が情報を運ぶか」を担当する。

**肉厚マップの明暗が逆だったのを直した — 薄肉が明るく、厚肉が暗くなった。**

肉厚が厚いほど明るい黄色、薄いほど暗い青、という向きだった。これは「インクが濃い＝量が多い」
という普遍的な読みと逆で、さらに透明樹脂の成形品では**厚いほど光が減衰して実際に暗く見える**
ため、現物を見慣れた目には二重に逆さまだった。

### 変更

- 肉厚マップの配色を `cividis` → **`cividis_r`** に反転。2D 設計図（`app.py` の「成形品設計図」）、
  3D 立体図（`render_3d_thickness_map`）、層別 thickness パネル（`render_layer_map` /
  `render_layer_grid`）の3箇所すべてに適用
- 配色を `core.visualizer.THICKNESS_CMAP` に単一ソース化。3D 側は Plotly の命名に合わせた
  `core.visualizer_3d.THICKNESS_COLORSCALE`（`"Cividis_r"`）を別に持ち、両者の整合はテストで担保
- 充填時間（`turbo`）・圧力（`magma` / `Turbo`）は**変更なし**。肉厚は解いた結果ではなく入力形状
  なので、単一量の明度 ramp のままにして結果図と見た目のカテゴリを分けている

### 検討して採らなかったもの

**単色系（`Blues` / `bone_r` 等）の「濃さ＝量」ramp**。比喩としては最も純粋だが、低端が白に寄る
マップは製品プレートを白飛びさせる。プレートは最薄領域であると同時に唯一注視される領域で、
`bone_r` では 0.35mm 帯が純白になって外形そのものが消え、3D では天面が薄グレーの PL 床と白背景に
溶けた。隣接ゾーン間（0.35 / 0.50）の段差コントラストも落ちる。`cividis_r` は薄肉端が彩度のある
黄で残るためこれを回避し、cividis が色覚多様性向けに設計されている性質も反転後に残る。

### テスト

15件追加（358 → 373）。配色は名前で照合せず、**実際に描かれる ramp を測色して**4つの性質を見る（以下の実測値は plotly 6.7.0 のもの。`plotly>=5.18` は上限無しなので、**判定基準は固定・期待値だけ追随**させる）。
名前だけの比較は `Cividis_r` が実は反転していなくても通ってしまうので、判定材料にならない。

| 性質 | 判定 | 落ちる例 |
|------|------|---------|
| 明→暗に単調 | ramp を stop 間まで密サンプルし、隣接サンプルが必ず暗くなる | `Blues_r`（反転）、`jet_r`（両端は明→暗、中間で 0.719 逆行）、`bluered_r`（stop 判定は通り区間内で 0.0023 逆行）|
| 情報を運ぶ | 薄肉端と厚肉端のコントラスト比 ≥ 3:1（WCAG 1.4.11）| 近黒→黒の ramp（1.00:1）、`jet_r`（1.44:1）|
| 白と見分く | 薄肉端の HSV 彩度 > 0.5 | `bone_r`（低端が純白）|
| 2D と 3D が一致 | `THICKNESS_COLORSCALE.lower() == THICKNESS_CMAP` | 3D だけ別配色にした場合 |

`cividis_r` の実測は 単調 ✓ / 12.56:1 / 彩度 0.78。**彩度で白と分けるのは意図的**で、`cividis_r` の
薄肉端（黄）は白に対して輝度では 1.25:1 しかない — 白背景から分離しているのは彩度であって明度
ではないので、明度で判定すると採用した配色自身を弾く。

密サンプルが要るのは、Plotly が符号化 sRGB で線形補間する一方 relative luminance が非線形変換を
先に掛けるから。伝達関数は凸なので、区間内の輝度は暗い側の端点より下に沈んでから戻りうる — 
**これは色空間の性質であって特定の配色の性質ではない**。だから証人は組込配色ではなく
**構成した反例**（緑→マゼンタ、端点 0.377→0.285 で単調に見えて途中 0.142 まで沈む）で固定した。
`bluered_r` も今は同じ挙動を示す（0.0023）が、それが変わっても密サンプルの必要性は変わらない。

基準そのものの判別力は `Cividis_r` / `Blues` / `Blues_r` / `Jet_r` / `Bluered_r` / `Greys_r` の
期待値表で固定した。定数を差し替える変異注入では名前一致 assert が先に落ちて判定にならないため、
基準を直接テストしている。色の計算は `tests/colorimetry.py` に集約 — **輝度の式を2ファイルに手で
書き写していて、両方が同じ間違いをしていた**のが集約の理由。

---

## [0.20.0] — 2026-08-19

**アイランドの一部を溶接で埋めて堰にした金型を、そのまま解けるようになった。**

実機の改訂図面で「アイランドの下流10mmを溶接で肉盛りし、流路厚みを 0.1mm 一定にする」
という改修が出た。既存スキーマではこれを表現できず、最寄りの近似（島を一定深さにする）
では中央-端の充填時間差が半分以下に潰れ、最遅点の位置まで変わる。0.1 と 0.35 は
コンダクタンスが h³ で 43 倍違うので、堰を堰でないもので代用した時点で改修の評価が
成立しない。

### 追加

- `island.weld`（任意）: `t_range` と `depth` を持つサブセクション。島の内側かつ
  `t_range` の帯にあるセルの深さを `depth` で上書きする。溶接金属はポケットを埋める
  ものなので `depth ≤ land.depth` を要求し、`t_range` は
  `[land.length, island.end_dist]` の内側でなければならない。入口・出口の段差は
  島自身の急峻な壁と同じくシャープカット近似。
- `WeldSpec` を `core` から export。
- `tests/test_geometry_profile_gate.py` に6件（帯内の一定深さ・帯外と島外が不変・
  体積減を求積と照合・未指定時は旧挙動と bit-identical・JSON round-trip・
  バリデーション5系統）。

### 修正

- **スペックJSONの数値フィールドが NaN / ±Infinity を受け付けていた**（Codex レビュー指摘、
  weld の2フィールドとして報告されたが同じ穴は既存の全数値フィールドにある）。
  `validate` の範囲チェックはすべて比較なので、NaN は**上限側も下限側も素通り**する。
  その先で深さ場に NaN が入れば体積も解もすべて NaN になり、mask の比較に使われれば
  条件が全部 False になって**指定した造形が黙って消える**（後者の方が悪い — 形状が違うのに
  気づけない）。
  防御点は `validate()` の**1箇所**に置いた。dataclass を再帰的に走査して数値リーフを
  すべて拾い、非有限なら弾く。パーサ側（`_num` / `_pair` / `_line`）で弾く案を最初に試したが、
  **spec の dataclass は export されていて JSON を通さず直接構築できる**ので入口を塞ぎ切れない
  （Codex の追加指摘）。走査は `dataclasses.fields` を辿るので、**後からフィールドを足しても
  自動で守られる**。
  走査のスカラ判定は `numbers.Real` で行う。`(int, float)` だと **NumPy スカラが黙って
  落ちる** — `np.float32` / `np.int64` は組み込み型のサブクラスではないので、
  最適化スイープや配列から読んだ値でスペックを組むと、走査のリーフ数が減ったまま
  検証をすり抜ける（`np.float64` はたまたま `float` のサブクラスなので捕まるが、
  それは偶然であって保証ではない。Codex の4件目の指摘）。
  テストは報告された箇所を名指しせず、(1) 走査がスペックの数値リーフ28個すべてに届くこと、
  (2) JSON 経由で NaN / +Inf / −Inf の3値すべてが拒否されること、(3) パーサを通さない
  直接構築（トップレベル / ネスト / 二重ネスト / タプル要素）でも拒否されること、
  (4) NumPy スカラ（float32 / float64 / int64）が走査から落ちず、有限なら受理・NaN なら
  拒否されること、を確認する。

- **`[a, b]` / `[[t,w],[t,w]]` のフィールドが、2要素なら何でも受け付けていた**
  （Codex レビュー指摘）。`a, b = val` は任意の2要素イテラブルを取るので、
  `"t_range": "68"` が `(6.0, 8.0)` と解釈され、**溶接帯が全く別の位置にある形状として
  黙って通る**。他の壊れた値（`[true, false]` 等）が弾かれていたのは型検査ではなく
  後段の範囲チェックに偶然引っかかっていただけで、`"68"` は `[land.length, end_dist]` の
  内側かつ増加なので何にも当たらない。実配列であること・要素が bool でない実数であることを
  要求する。テストは5つの配列フィールドすべてを走査して、壊れた値8種を拒否することを見る。

### 変更

- なし。`island.weld` を持たないスペックの挙動は完全に不変（テストで担保）。

---

## [0.19.0] — 2026-08-19

**ショートショットが「時間がかかる」に化けるのをやめた。**

スキン層モデルが流路を閉じたセルは、数値安定のため `min_core_thickness_mm`
（既定 0.01 mm）で床打ちされる。閉じたセルの τ は他より桁違いに大きく、それが
`tau_max` になって**絶対時間の基準を決めていた**。結果、

- 総充填時間 T_fill が実際の数倍に膨らむ（実測: 0.115 s → 正しくは 0.031 s）
- カラーバーの上位 73% に1セルも存在しない — 全セルが下端に潰れる
- フレームは時間等分なので、60コマ中40コマ以上が何も起きない消化試合になる

**充填しないセルは充填時間を持たない**、に変えた。

### 変更
- **時間軸は充填するセルの τ から取る**（`tau_max_flow`）。凍結セルとその後ろに
  封じ込められた領域は基準に入れない。スキン層 OFF では従来と完全に同一
- **凍結セルは壁**として扱い、ゲートから到達できなくなった領域も「充填されない」に
  含める（`scipy.ndimage.label` による連結成分判定）。局所の肉厚ではなく、
  ゲートへの連結性が決める
- `FlowResult.unfillable_mask` を追加。該当セルの `fill_time_s` / `pressure_norm` は
  **NaN**（大きい数値ではない。数値を入れると「いつかは到達する」という意味になる）
- 充填アニメで未充填セルは専用色（`SHORT_SHOT_RGB`、ショートショットマップと同じ赤）で
  最後まで残る。`filled` に何を渡されても露出しない
- フレームのタイトルに `short shot: N cells` を出す。充填率は 1 桁表示なので、
  数セルのショートショットは "filled = 100.0 %" に丸め込まれて消える
- metadata に `unfillable_cells` / `sealed_off_cells` / `tau_max_flow`
- `tests/test_short_shot_timeline.py`（30件）

### 実装メモ
- **色の場は「有限値を持つセル」から外挿する**（キャビティ mask からではない）。
  NaN セルを代替値で埋めて mask から広げると、その値が隣の生きたセルに滲む
- 未充填セルを覆う判定は `filled` に依存させない。今は `NaN <= t` が False なので
  結果的に覆われるが、それは NaN 比較の性質であって設計判断ではない
- **層別ソルバーのショートショット判定（中央層温度ベース）はこの扱いに含めない**。
  あちらはコンダクタンスを床打ちしないので τ の病理が無く、指標として出すに留める

### 実装メモ（レビュー指摘）
- **ウェルド／エアトラップは充填するセルだけで計算する**。凍結セルの τ は他より
  桁違いに大きく、それは局所極大＝エアトラップ判定が探している形そのものなので、
  **樹脂が来ない場所に不良マークが立つ**。テストは「生 τ なら実際に立つ」ことを
  前提として assert してから「修正後は立たない」を見る（前提を書かずに書いた初版は、
  死領域の極大が境界上にあって判定範囲外だったため、何も観測せずに通っていた）
- **等値線は有限な充填時間が2種類未満なら描かない**。凍結セルがゲート以外を全部
  封じ込めると有限値がゲートの 0 だけになり、`contour` が
  `ValueError: Contour levels must be increasing` で落ちる。完走した解析を
  飾りのために失うのは割に合わない
- **何も流れないケースを独立に扱う**。凍結セルがゲート以外を全部封じ込めると、
  「充填するセル」に残るのはゲートの τ=0 だけになる。ここで全体最大に
  フォールバックすると**排除したはずの死セルの τ が戻ってくる**（実測: T_fill が
  baseline の 64 倍、一方で描画側は 1.0 秒固定にフォールバックしていて、
  同じ結果に矛盾する2つの時間軸が出ていた）。`_tau_reference` は使える参照が
  無ければ `None` を返し、solve は T_fill をベースラインのまま据える
  （metadata に `no_flow`）。`fill_time_max` のフォールバックも 1.0 秒ではなく
  `total_fill_time_s` に変えて、軸とヘッドラインを一致させた
- **未充填領域を切り離してから τ を解き直す**。最初の解は cavity 全体で解いて
  いるので、封じ込められた領域も単位ソースを出し続け、凍結した帯も
  `min_core` の床コンダクタンスで通してしまう。**死んだ領域の体積が上流の
  生きたセルを通過することになり、その τ が膨らむ**（帯で封鎖した strip で
  実測 3.3 倍）。生きた領域だけの部分問題として解き直し、以降の充填時間・圧力・
  ウェルド・エアトラップは全部その解から取る
- **ベースライン時間も充填する体積で計算する**。樹脂が到達しない体積に時間は
  かからない。`_baseline_fill_time(geom)` を Geometry 引数に切り出し、圧縮の
  実効係数も生きた領域で評価する
- **射出率を指定しない既定経路も体積でスケールする**。`injection_volume_flow_cm3s`
  が `None` のとき、既定は「図面どおりのキャビティを 1.5 秒で充填」＝**率**であって
  所要時間ではない。時間として読むと、体積が1割しか残らないショートショットにも同じ
  1.5 秒を渡すことになり、live 体積スケーリングが既定経路でだけ効かなくなる。
  `DEFAULT_FILL_TIME_S` から元のキャビティ体積で率を導いて使う
  （制限後のソルバに率を聞くと、自分の体積で割り戻して既定値に戻ってしまう）
- **キャビティとスキン層の不動点は同時に決める**。スキンの厚さは到達時間で決まり、
  到達時間は「どのキャビティを解いたか」で決まる。全体キャビティで一度だけ
  不動点を回し、その `h_core` を使い回して τ だけ解き直すと、**生きた領域の
  スキン層が死んだ体積に依存したまま残る**。同じ live 形状の後ろに置く死領域を
  20セルと100セルで変えると、報告される充填時間が **3.9 倍**ずれた（掃引全体では
  0.31〜8.28 倍）。生きた領域を切り出したら**そこでスキン層の不動点を解き直し**、
  新たに凍結したセルがあればもう一度切り出す。領域は縮む一方なので必ず停止する
  （`MAX_DOMAIN_PASSES = 4` は暴走止め、実測 1〜2 回で収束）。修正後は上の掃引
  全ケースで比が **1.0000**、live 領域の `h_core` と `fill_time` は死領域の量に
  対してビット単位で不変
- `_restricted_to` は射出率を**現在値でピン留めした**コピーを返す。制限は
  キャビティを変えるのであって成形機を変えるのではない（暗黙の率を残すと、
  縮んだ体積を同じ縮んだ体積由来の率で割って制限が自己相殺する）
- metadata に `domain_passes`（切り出しの回数。0 なら凍結なし）
- **inflation は同じ領域の2状態を比べる**。生きた領域の「スキン込み」と
  「スキン無し」の τ_max 比。cavity 全域の参照と比べると、形状変化が
  スキンの指標に混ざる
- **時間軸は1箇所で決める**。色スケール・タイトル・フレーム時刻がそれぞれ
  `fill_time_s` の最大を自前で計算し、それぞれ 1.0 秒へフォールバックしていた。
  片方だけ直しても残りが別の時間を語る（実測: ヘッドライン 0.023 秒に対して
  アニメは 1 秒で進行）。`fill_frame_times` も `fill_time_max` を通す
- **fixed-point ループはゲート以外が全部凍った時点で抜ける**。次の反復が
  `None` を除数にして `TypeError` で落ちていた（答えは既に出ているのに、
  描画どころか solve が死ぬ）。実測で c_skin=3.0・薄肉 0.03mm 等で再現
- **圧力マップは未充填セルを明示的に塗る**。NaN はカラーマップの bad 色
  （透明）になるが、直後に cavity のアルファを 1 に固定するので**真っ黒**になり、
  「圧力が最低」に読める。実測で magma 下端色として 8,257 画素出ていた

### 既知のトレードオフ
- **T_fill の表示値が変わる**（凍結セルがある場合のみ）。従来値は「凍って充填され
  ないセルが、無理に充填されるとしたらこの時間」であって、製品の充填時間ではない。
  充填不良を所要時間に化かすより、充填する範囲の時間＋未充填セル数を出す方が安全

---

## [0.18.0] — 2026-08-18

**ダウンロードした ZIP から、その結果を出した設定を復元できるようになった。**

ZIP に `settings.json` を同梱した。`metadata.json` は解いた結果（体積・τ・反復回数）
しか持っていなかったので、後から「この結果はどの設定で出したのか」を辿る手段が
無かった。実際、過去の ZIP の形状を特定するのに、画像から寸法を測り、体積と
`tau_max` が全桁一致する config を総当たりで探す羽目になった。

### 追加
- **`settings.json`**（ZIP 同梱）— 形状 config の全フィールド、材料、射出条件、
  壁面冷却モデル、圧縮成形、出力設定、そして解析時の版・コミット。キー名は UI の
  ラベルではなく dataclass / solver の引数名（UI 文言の変更に影響されず、CLI の
  引数にそのまま対応する）
- `core/settings_record.py` — `config_settings()` / `file_fingerprint()` /
  `settings_json()`
- 結果ペインに「この結果を出した設定」expander
- `tests/test_settings_record.py`（10件）

### 実装メモ
- **アップロードしたスペック JSON は名前と SHA-256 だけを記録し、中身は載せない**。
  ゲートプロファイルは実図面由来のことが多く、この ZIP は人に渡す前提で作って
  ある（`player.html` を同梱しているのがまさにその用途）。フィンガープリントなら、
  スペックを持っている側が「どの版で回したか」を確認できる一方、寸法は ZIP と
  一緒に流れない。同じ理由で画像入力もファイル名とハッシュだけ記録する
- 形状 config は手書きのキー列挙ではなく `dataclasses.asdict` で丸ごと写す。
  テストも dataclass 自身と突き合わせるので、**新しい形状パラメータを足したのに
  記録し忘れる**ことが起きない
- 漏洩テストは「シリアライズ結果に実際の数値が現れないこと」を見る。キーの
  有無を見るテストだと、将来中身を埋め込む変更が入っても素通りする
- **スペック自身の `name` フィールドも「中身」**なので載せない。初版はこれを
  `spec_name` として記録していた（レビュー指摘で削除）。ファイル名とハッシュだけを
  出す方針を立てておきながら、隣の行で中身の1フィールドを写していた。`spec` の
  下に来るキーは `name` / `sha256` / `bytes` だけであることをテストで固定した

---

## [0.17.0] — 2026-08-18

**充填アニメーションが商用 CAE と同じ読み方でできるようになった。**

配色を虹系（turbo）に変え、等時線を重ね、色だけを滑らかに補間するようにした。
輪郭と溶融先端はセル厳密のまま。

### 追加
- **等時線オーバーレイ** — 同時に充填される位置を結んだ等値線を既定12本
  （N を指定すると N 本ちょうど）。線が
  詰まる=流れが遅い、ぶつかる=ウェルド、途切れた先=最後に充填。色覚に依存せず
  白黒印刷でも残る、この図の定量的な読み。UI のスライダーで 0〜24 本
- **配色セレクタ** — turbo（既定）/ jet / viridis / cividis。赤緑色覚への配慮は
  cividis / viridis の選択で担保する
- **フレーム PNG にカラーバー** — プレイヤーと `player.html` は周囲に説明が無いので、
  各フレームが単体で凡例を持つ必要がある
- `tests/test_fill_render.py`（24件）

### 変更
- **充填アニメの既定配色を `viridis` → `turbo`**。成形屋がこの図から読むのは
  絶対値ではなく等時線の形で、虹の色相コントラストがその縞を浮かせる（明度が
  単調な viridis は逆に均してしまう）。赤=最後に充填=リスク箇所という意味論とも
  一致する。`jet` ではなく `turbo` なのは、jet がシアンと黄で明度が跳ねて
  **データに無い偽の縞**を描き、本物の等時線と紛れるため
- **色の層だけを `bilinear` 補間**するようにした。`_nearest_extend` でキャビティ外を
  内側の最近傍値で延長してから色を作り、その上に未充填セルを `nearest` の不透明
  オーバーレイで punch out する。**充填時間は連続場なので色の補間は嘘にならないが、
  キャビティ輪郭と溶融先端は mask のとおりが正しい**ので、アルファに mask を持たせて
  補間する（＝先端が半セル滲み、細い流路が痩せる）ことは避けた
- `render_fill_animation` と `export_frames` に重複していた RGBA 構築とゲートマーカー
  描画を共有ヘルパーに集約

### 実装メモ
- **等時線はキャビティ全体に一度だけ描き、未充填オーバーレイで覆って隠す**。
  フレームごとに引き直すと `ContourSet.remove()` が要るが、これは Matplotlib 3.8
  以降にしか無い（本パッケージの下限は 3.7）。描き直しをやめたことで版依存が
  消え、コンターの計算もフレーム数ぶんから1回に減った
- ゲートマーカーはオーバーレイより上（`_Z_GATE`）。マーカーは1セルより大きいので、
  最初のフレームやキャビティ境界上のゲートでは不透明な未充填塗りに食われる。
  **`_draw_gate_markers` の既定値**にしてあるので、呼び出し側が渡し忘れられない
- 1セル幅のキャビティ（1ピクセル幅の画像アップロード）では等時線を描かない。
  `contour` は 2x2 を要求するが、解析自体は成立するので、飾りのために完走した
  結果を捨てるのは割に合わない
- **`export_frames` も GIF と同じく figure を一度だけ組む**。フレームごとに
  作り直すと PNG 1枚につき contour を1回引くことになる（UI 既定の60枚、UI が
  許す 500k セルのキャビティで実測 41 ms/回 = 約 2.5 秒ぶん）。出力される PNG は
  どちらでも同一なので、**contour の呼び出し回数を数える以外に検出手段がない**
- 重ね順は `zorder` で明示する。matplotlib は `imshow` を 0、`contour` を 2 で
  描くので、オーバーレイを後から呼ぶだけでは等時線が上に残り、未充填部に線が
  透ける（実際にそうなった。呼び出し順を見るテストはこれを通してしまい、
  PNG を目視して初めて分かった）

### 既知のトレードオフ
- **溶融先端は階段状に見える**。セル解像度そのものなので、滑らかにすると
  存在しない位置に先端を描くことになる。滑らかにしたいときはメッシュを細かくする
- **肉厚マップにこの平滑化を横展開してはいけない**。肉厚は本質的に不連続で
  （t0.35 と t0.50 の間に中間肉厚は実在しない）、補間すると存在しない肉厚が
  あるように見える。3D の flat-top 判断と同じ原則。肉厚の配色は別途検討
- 虹系は赤緑色覚異常（男性の約8%）に不利。既定を虹にしたうえで、選択肢として
  cividis / viridis を残すことで対処している

---

## [0.16.0] — 2026-08-18

**ダウンロードした ZIP を渡された相手が、追加ソフト無しで画面と同じコマ送りを使えるようになった。**

ZIP に単体 HTML プレイヤー `player.html` を同梱した。ダブルクリックすれば既定の
ブラウザで開き、アプリ内と同じシーク・コマ送り・速度切替が使える。フレームは
data URI で埋め込まれているのでオフラインで完結し、外部通信もストレージ書き込みも
一切しない（HTML はブラウザのサンドボックス内で動くだけで、レジストリや拡張子の
関連付けには触れられない）。

### 追加
- `fill_player.wrap_standalone_html()` — 埋め込み用フラグメントを完全な HTML
  文書に包む。`<meta charset="utf-8">` を最初の非 ASCII バイトより前に置くのが
  要点で、これが無いと `file://` で開いたブラウザがプラットフォーム既定の
  レガシー encoding（日本語 Windows なら CP932）にフォールバックしてボタン
  ラベルが文字化けする
- ZIP に `player.html` を同梱。ページ見出しに `build_label()` の版・コミットを
  出すので、受け取った側もどのビルドの結果かを判別できる
- `tests/test_fill_player.py` に 7件追加（charset 位置、文書完全性、フラグメント
  同一性、オフライン自己完結、title/note のエスケープ、プレースホルダ残留）

### 既知のトレードオフ
- **ZIP がおよそ 2 倍になる**。base64 は元の 4/3 に膨らむうえ PNG は既に圧縮済みで
  deflate が効かないため、実測でフレーム 24 枚のケースが 310 KB → 628 KB。
  フレーム PNG 一式がもう一部増えるのと同等
- 企業のメールゲートウェイが HTML 添付を隔離することがある。その場合でも
  `frames/` の連番 PNG が残るので、標準ビューアの矢印キーでコマ送りはできる

---

## [0.15.0] — 2026-08-18

**充填アニメーションを任意の位置で止めて見られるようになった。**

自動再生されるだけの GIF を、ドラッグでシークできる HTML5 プレイヤーに置き換えた。
フレーム PNG は以前から書き出していた（ZIP 同梱用）ので、レンダリングコストは増えていない。

### 追加
- `core/fill_player.py` — `build_fill_player_html()`。フレーム PNG を data URI で
  埋め込んだ自己完結 HTML プレイヤー。再生 / 一時停止、ドラッグシーク、コマ送り、
  速度切替（0.25〜2x）、ループ切替、キーボード操作（Space / ← / →）。操作は
  すべてブラウザ内で完結し、シーク中にサーバへ戻らない
- `visualizer.fill_frame_times()` / `fill_frame_fractions()` — GIF・PNG 連番・
  プレイヤーが共有するフレーム時刻の単一ソース
- `tests/test_fill_player.py`（8件）

### 変更
- UI の「充填先端アニメーション」が GIF ではなくプレイヤーを表示する。GIF は
  従来どおり ZIP に同梱（ダウンロード用途は変わらない）
- `render_fill_animation` と `export_frames` に重複していたフレーム時刻の
  `linspace` を `fill_frame_times()` に集約。3系統目のプレイヤーが同じ式を
  持たないようにするための整理

### 既知のトレードオフ
- プレイヤーは `st.components.v1.html` の iframe 内で動くため Streamlit の
  テーマ色を継承しない。明暗どちらでも読める中間色で組んである
- 全フレームを data URI で埋め込むので、既定 60 フレームで HTML が約 1.7MB になる

---

## [0.14.0] — 2026-08-07

**JSONスペックで外部からゲート形状を持ち込めるようになった。**

図面から抽出した深さ場モデルを JSON で受け取り、キャビティ形状にラスタライズする
4系統目のジオメトリ。これまでのようにビルダーをコードで書き足さなくても、
新しいゲート形状を解析にかけられる。

### 追加
- `GateProfileSpec`（JSON入出力つきスペック定義）と `build_profile_gate_geometry`
  — ランド／メインランプ（底打ちあり）／浅いアイランド／外壁線／オブラウンド井戸／
  バルブオリフィスの合成による深さ場。左右対称・片側の両モード対応
- Streamlit UI に「Profile gate (JSONスペック)」系統（デモプリセット／アップロード／貼り付け）
- 架空寸法のデモスペック `data/gate_profiles/demo_profile_gate.json`
- テスト28本（閉形式の体積検算、井戸体積の放射積分照合を含む）

### 修正
- JSON のセクション値がオブジェクトでない場合に `TypeError` が UI のエラー処理を
  すり抜けていた問題（パス付きの `ValueError` に統一）
- ゲート形状がラスタ格子をはみ出す場合に無言で切り詰められ、誤った体積・充填結果に
  なっていた問題（事前検証で明示的に拒否）

### 変更
- `well.floor_t_range` を任意項目化し、図面参照用のメタデータであることを明記
  （実際の床形状は深さと壁角から導出される）
- CI: `docs/` を ruff の対象から除外（新しい ruff が Markdown 内のコード片を
  整形対象にするようになったため）

---

## [0.13.0] — 2026-06-09

**3D表示が軽くなり、肉厚の段差が段差として正しく見えるようになった。**

### 変更
- 天面・床の描画をフルグリッドの `Surface` から、キャビティセルだけを張る
  疎な `Mesh3d` に置換 — 回転・ドラッグの負荷を大幅に削減
- 天面を「厚み一定の平らなブロック」の集合として描画するよう変更。隣接セルで
  肉厚が変わる境界には段差壁を立て、設計上の段差を垂直な段差として忠実に再現
  （従来はセル中心の補間で1セル幅の傾斜に均されていた）
- メッシュ粗さの下限を 0.5 → 0.2 mm/cell に開放。併せてセル数の上限ガードを追加
  （クラウド環境でのメモリ枯渇を防止）

### 削除
- 表示専用のアップサンプリング機能、および表示専用の解析的精細化機能
  — いずれも描画コストに見合う効果が得られず撤回

### 既知のトレードオフ
- 平らなブロックで描くため、意図的に連続なテーパ（ランナーのスロープ等）は
  微細な階段状に見える。設計段差との判別が原理的に困難なため、製品面の段差を
  正確に見せることを優先している

---

## [0.12.0] — 2026-06-07

**パラメータが増えても迷わないUIになった。**

### 変更
- 入力UIを大項目・中項目・小項目の折りたたみ階層に全面再構成。起動時は
  すべての大項目を畳んだ状態にし、必要な箇所だけ開く形に
- テーパ段化を OFF にした場合、全幅で完全に同一の単一テーパになるよう修正

### 見送り
- アクセント色のカスタマイズ — Light/Dark テーマ切替と両立できないため撤回

---

## [0.11.0] — 2026-06-06

**肉厚調整ゲート（直角台形）を扱えるようになった。**

### 追加
- 3系統目のジオメトリ「Film gate 2（ゲート位置可変）」
  — 直角台形のフィルムゲート、ゲート位置の可変化、2段テーパ、深ランナー、
  ランド境界の段差指定に対応

---

## [0.10.0] — 2026-06-05

**社外に出せる解説資料が揃った。**

### 追加
- 顧客向けの流動解析解説資料5本をリポジトリ管理下に統合
  （解析の仕組み、商用CAEとの違い、プログラム解説ほか）

### 変更
- 支配方程式の記述を実装に合わせて修正（拡散方程式 → ポアソン方程式）
- 金型冷却解析の扱いについて、資料間で線引きを統一

---

## [0.9.0] — 2026-05-15

**粘性発熱を考慮できるようになり、結果一式をまとめて持ち出せるようになった。**

### 追加
- 剪断発熱（粘性散逸）の補正モデル。ブリンクマン数を常時算出し、
  発熱の影響度を信号灯で提示
- アニメーションGIFと全フレームPNGを1つのZIPで一括ダウンロード

### 変更
- 圧縮成形（ICM）のUIをストローク指定に一本化
- 壁面冷却モデルの呼称を整理し、極薄プレート向けの既定値に調整

### 修正
- クラウド環境で材料データベースのキャッシュが更新されず、
  スキーマ変更後に不整合が起きる問題

---

## [0.8.0] — 2026-05-13

**板厚方向の温度・粘度分布を解く層別ソルバーが入った。物理エンジンの世代交代。**

これまで単層として扱っていた板厚方向を N 層に離散化し、各層の温度・粘度・剪断速度を
連成させて解く新ソルバー。壁面近傍の冷却と中央部の流動を分離して評価できる。

### 追加
- `MultilayerHeleShawSolver` — 層別の温度評価（1次元熱伝導解）、層別粘度、
  収束計算による連成。N=1 で従来ソルバーと厳密に一致
- 壁面を細かく刻む層分布、中央層の温度に基づくショートショット判定、
  収束の適応的減衰
- 層別プロファイルの可視化（温度・粘度のグリッド表示、ショートショットマップ）

### 追加（圧縮成形）
- 圧縮量をストローク（絶対量）で指定するモード
  — 段差のあるプレートで段差を保ったまま圧縮できる

---

## [0.7.0] — 2026-05-08

**ダイレクトゲートに対応し、理論的な裏付けを画面上で説明できるようになった。**

### 追加
- 2系統目のジオメトリ「Direct gate」— 製品内部に直接ゲートを配置する形式。
  プレートの2層分割にも対応
- 支配方程式と適用範囲の解説（数式表示つき）を画面上に追加
- 材料データベースにタルク強化ポリプロピレン3種（PP-T10 / T20 / T30）を追加

### 変更
- 射出率スライダーの既定値を実機相当に調整
- 画面上の表記を対外説明に耐える文体・用語に統一

---

## [0.6.0] — 2026-05-08

**結果を立体で見られるようになった。**

### 追加
- Plotly による3D可視化（肉厚マップ／充填時間／圧力）
  — パーティングライン基準の押し出し表現、側壁の着色、実寸等倍表示

---

## [0.5.0] — 2026-05-06

**誰でもブラウザから触れる公開アプリになった。**

### 追加
- Streamlit Community Cloud へのデプロイ設定と公開URL
- 解析結果（GIF等）のダウンロード機能

### 変更
- 射出率・圧縮成形の既定値を実機の運転条件に合わせて再設定
- 解析実行ボタンをヘッダー行へ移動

---

## [0.4.0] — 2026-05-06

**流動バランスの設計検討ができるようになり、壁面の凝固層を物理モデルに取り込んだ。**

### 追加
- フローバランサー（中央を意図的に薄くして樹脂を外側へ振り分ける肉盗み）。
  最大5段のネストに対応
- 壁面凝固層（スキン層）モデル — 壁から成長する固化層でコアの流路が狭まる効果を
  収束計算で評価。流路が閉塞した箇所はショートショットとして提示
- プレートをゲート側／反ゲート側の2つの肉厚帯に分割する機能

---

## [0.3.0] — 2026-05-05

**ゲート付きの実形状を入力できるようになった。**

### 追加
- 1系統目のパラメトリックジオメトリ「Film gate」
  — 台形ランナー＋半円部＋バルブゲートの平面形状を寸法指定で生成
- Streamlit UI と CLI デモの両方に配線

---

## [0.2.0] — 2026-05-05

**解析結果の正しさを自動で検証できるようになった。**

### 追加
- 1次元ストリップ流れの解析解と数値解を突き合わせる検証テスト
- GitHub Actions による CI（lint・フォーマット・テストを Python 3.11 / 3.12 で実行）

### 変更
- ruff によるコード整形を全体に適用

---

## [0.1.0] — 2026-05-05

**最初の動くシミュレータ。**

### 追加
- Hele-Shaw 近似 + Cross-WLF 粘度モデル + 疑似伝導法による充填時間場ソルバー
- 充填アニメーション、圧力マップ、ウェルドライン／エアトラップ推定
- Streamlit UI と CLI バッチ実行
- MIT ライセンスでの公開、pyproject.toml ベースのパッケージング

[0.14.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.14.0
[0.13.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.13.0
[0.12.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.12.0
[0.11.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.11.0
[0.10.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.10.0
[0.9.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.9.0
[0.8.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.8.0
[0.7.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.7.0
[0.6.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.6.0
[0.5.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.5.0
[0.4.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.4.0
[0.3.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.3.0
[0.2.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.2.0
[0.1.0]: https://github.com/shostako/mold-flow-sim/releases/tag/v0.1.0
