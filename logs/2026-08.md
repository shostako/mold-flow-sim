# 2026-08 作業ログ

## 2026-08-07 作業概要

### Profile gate（JSONスペック駆動）系統の追加 — PR #45 マージ済み

gokuusu セッションで実図面PDF（巴山さん設計・肉厚調整ゲート2案、2026/7/3）から抽出した
深さ場モデルを解析可能にする **5系統目のジオメトリビルダー**。引き継ぎ資料
`gokuusu/docs/ゲート形状抽出_mold-flow-sim引き継ぎ_2026-08-07.md` の要求を全消化。

**機密境界**: 本リポは public。入れたのは形式定義・ビルダー・UI・**架空寸法デモJSON**のみ。
実寸スペックは gokuusu 配下に置き、UIでローカル読込する運用。

#### 実装（`core/profile_gate.py` 新設 696行）

geometry.py が既に1339行 + JSON I/O は異質な関心事なので**別モジュール**にした
（`from .geometry import Geometry` でコンテナだけ借り、builder の12ステップ定型は完全踏襲）。

- **`GateProfileSpec`**: JSON構造を鏡写しにした**ネスト frozen dataclass**（既存Configは全部
  フラットだが、それはスライダーUI駆動だから。JSON駆動なら round-trip・エラーメッセージ・
  省略可能セクションの全てでネストが有利）。`from_dict/from_json/from_json_file/to_dict/
  to_json/validate/t_max/ramp_cap_t`。エラーは `"island.boundary_line"` 形式のパス付き、
  **未知キーは拒否**（実寸JSONのタイポ検出）。
- **`ProfilePlateConfig`**: スペック（図面由来・不変）とプレート（UIで振る）は寿命が違うので分離。
- **`build_profile_gate_geometry(spec, plate, cell_size_mm)`**: 全numpyベクトル化。
  - ランド/メインランプ(cap底打ち)/アイランド/外壁線/井戸/バルブの合成
  - **井戸だけ距離場**（60°壁が体積に効くので `d_well = clip((hw−dist)·tanθ, 0, depth)`、
    `d = max(d, d_well)`）。アイランドの急壁はセル解像度1mm前提でシャープカット近似
  - `mask = in_gate_base | in_well | in_plate` の **OR が必須**（実寸案では井戸が外壁を突き抜ける）
  - 外壁線の t<始点側は全幅クランプ（外挿するとランド帯が狭まる罠）
  - `compression_mask = in_plate & mask`、ゲート土手、gates空時の最寄りセルスナップ防御
- UI（app.py）: デモプリセット/JSONアップロード/JSON貼り付けの3択 + プレートパラメータ
- CLI（run_demo.py）: `ProfileGate_demo` ケース

#### レビュー対応（Codexがトークン切れ → Claude `/code-review 45 high` で代替）

4件 CONFIRMED、全部正当だったので全対応:

| 指摘 | 対応 |
|------|------|
| セクション値が非dictだと `TypeError`（app.pyのValueErrorハンドラをすり抜けて生traceback）。`"land": "abc"` は文字単位の「unknown key ['a','b','c']」という珍メッセージ | `_section()` ヘルパーで isinstance チェック → パス付きValueError |
| ポケットがラスタグリッドをはみ出しても**無言で切り詰め**（体積・充填が誤りになるが診断なし）。一方 `gate_exit_width > plate_w` は弾いており基準が不整合 | x範囲を事前検証してValueError。実寸2案・デモは通ることを確認 |
| `well.floor_t_range` がパース・検証されるだけで**ラスタライザ未使用**（床は depth+wall_angle から導出される） | optional化し「図面参照メタデータ」と docstring 明記。範囲の妥当性検証だけは残す（抽出タイポ検出） |
| `PROFILE_GATE_CASES` が**import時に**デモJSONを読む（欠損すると無関係な全ケースが道連れ） | case実行時の遅延ロードに変更（`spec_path` を持たせる） |

#### 検証

- テスト28本（`tests/test_geometry_profile_gate.py`）、全体 **246 passed + 1 skip**
  - 閉形式体積検算（直壁最小スペック、単位幅断面積42.26mm²×幅200 vs ラスタライズ）±3%
  - 井戸差分を**放射積分** `∫(2L+2πr)·gain(r)dr` と照合 ±5%
  - JSON round-trip / 非dictセクション / グリッドはみ出し / 対称性 flip 比較 ほか
- `run_demo.py --cases ProfileGate_demo` で出力一式生成
- **Streamlit AppTest** でUI経路（Profile gate選択→解析実行）をヘッドレス検証
- **実寸2案をローカル（/tmp）で通し、ゲート体積が図面手積分値に対し −1.7% / −0.6%**

#### CI事故: 新版ruffがMarkdown内コードブロックを整形するようになっていた

初回CIが `docs/流動解析の仕組み_プログラム解説.md` の format check で落ちた。main の最終green
以降に ruff（CIは `>=0.5` の緩ピン）が更新され、md内のコードスニペットを整形対象にした
**事前故障**。docs は解説プローズなので `extend-exclude` に `docs` を追加して解消。

### 残
- 本番（Streamlit Cloud）反映は次デプロイ時。Profile gate 系統の目視は未実施。

---

## 2026-08-18 作業概要

### 充填アニメーションをシーク可能なHTML5プレイヤーに置換（PR #47 / v0.15.0）

自動再生されるだけの GIF 表示を、ドラッグで任意位置に止められるプレイヤーに差し替えた。

- `core/fill_player.py`（新規）— `build_fill_player_html()` / `fill_player_height_px()`。
  フレーム PNG を data URI で埋め込んだ自己完結 HTML。再生・一時停止、ドラッグシーク、コマ送り、
  速度 0.25〜2x、ループ、キーボード（Space / ←→）。**操作は全部ブラウザ内で完結しサーバに戻らない**。
- `visualizer.fill_frame_times()` / `fill_frame_fractions()`（新規）— GIF・PNG連番・プレイヤーが
  共有するフレーム時刻の単一ソース。
- GIF は従来どおり ZIP に同梱（ダウンロード用途は不変）。UI 表示だけ差し替え。

**追加コストがゼロだった理由**: `export_frames` が以前から ZIP 同梱用に全フレーム PNG を書き出していた。
既定60フレームで PNG 合計 1.26MB、base64 化して約1.7MB。

**重複の解消**: フレーム時刻の `linspace` が `render_fill_animation` と `export_frames` の2箇所に重複していた。
プレイヤーで3箇所目を作れば必ずズレるので、先に集約してから使った。3者のフレーム k が同じ瞬間である
ことをテストで固定（`tests/test_fill_player.py` 11件、全体 262 passed / 1 skipped）。

**検証で潰したUIバグ2件**（どちらもテストは通ったまますり抜けるやつ）:
1. **豆腐グリフ** — コマ送り/ループに U+23EE / U+23ED / U+1F501 を使い、絵文字フォントのない環境で□になった。
   Geometric Shapes（◀▶）＋日本語ラベルに置換。ローカル起動の目視で発見。
2. **iframe幅による操作列の切れ（Codex P2）** — `components.html` の高さは固定なのに iframe 幅は列幅に追従する。
   列が画像ネイティブ幅(700px)を超えると 7:5 の画像が縦に伸びて固定高さを超え、`scrolling=False` と相まって
   **操作列が画面外に出てプレイヤーが操作不能**（幅1100pxで889px必要／604px確保）。
   → `#stage` をネイティブ幅で頭打ち＋高さを PNG 実寸から導出（`app.py` のハードコード `int(700*(5/7))` を削除）。
   幅1100pxで操作列下端570px＜604px を実測確認。回帰テスト3件追加。

**教訓**: 1 はテスト緑・2 はローカル目視も通過した。**「画面に出て動いているのを見た」は「幅を振って確かめた」ではない**。
レスポンシブな埋め込みは寸法を変えて確認する。

### 圧縮成形のスコープ確認（コード変更なし）

「圧縮ストロークが効くのは製品部分だけか」の確認。`solver._open_thickness_field()` は
`compression_mask & mask` にだけ stroke を加算し、`profile_gate.py` は `compression_mask = in_plate & mask`。
実測でもプレートセルは全て +stroke、ゲートブロックのセルは Δh=0.000（1セルも動かない）。
Film gate / Direct gate / Film gate2 も同じ流儀。**例外は `build_demo_geometry` と `geometry_from_image` で、
`compression_mask=None` のため全セルが膨張する（旧挙動）**——画像から形状を作った場合はランナごと膨らむ。

副作用として、`effective_factor` は**分子が圧縮対象面積、分母が全キャビティ体積（ゲート部込み）**なので、
ランナが太いほど圧縮の効きが薄まる（意図的な設計）。ランナが製品と同体積規模だと効きが3割方削られる。

---

## 2026-08-19 作業概要（続き）

### 肉厚マップの配色を反転（v0.21.0, PR #54）

ユーザー指摘：「肉厚が厚い方が黄色で薄い方が青。色から得られる印象が逆転している」。**反論は無かった** —
むしろユーザーが挙げたより強い理由が2つある。

1. **「インクの濃さ＝物質の量」は普遍的な読み**。厚みは材料の量なので、多い方が濃いのが自然。
2. **透明樹脂の成形品では物理的にそうなる**。厚いほど光が減衰して暗く見える。LGP を毎日見ている目には、
   厚肉＝明るい黄色は現実と逆。
3. 充填時間(turbo)・圧力(magma)と役割が被っていた。**肉厚は結果ではなく入力形状**なので、
   結果図の虹ではなく単一量の明度 ramp を当てて一目で別カテゴリに見せるべき。

本体の変更は `cividis` → `cividis_r` の実質1行。適用は3箇所（2D設計図 `app.py` / 3D `render_3d_thickness_map` /
層別 thickness パネル）。3D は元々 `Viridis` で 2D と別物だったのでそこも揃えた。

#### 単色系（Blues / bone_r）を却下した理由 ← 実測で決めた

「濃さ＝量」の比喩としては最も純粋だが、**低端が白に寄るマップは製品プレートを白飛びさせる**。
プレートは**最薄領域であると同時に唯一注視される領域**という非対称があるため致命的。
`bone_r` は 0.35mm 帯が純白になり**外形そのものが消えた**（比較画像で確認）。3D では天面が
薄グレーの PL 床と白背景に溶けるのでさらに悪い。

`cividis_r` は薄肉端が彩度のある黄で残るのでこれを回避でき、cividis の色覚多様性対応も反転後に残る。

**重要な非対称**: `cividis_r` の黄は白に対して**輝度では 1.25:1 しかない**。白背景から分離しているのは
**彩度**であって明度ではない。だから「薄肉端は十分明るいこと」という明度ガードを入れると
**採用した配色自身が落ちる**。彩度で白と分け、コントラスト比で情報量を担保する、と役割を分けた。

### Codex レビュー9周 — 全部同じ欠陥クラスだった

指摘6件＋自己発見2件。**本体1行に対してレビュー由来のコミットが8本**。

| 周 | 指摘 | 「たまたま正しかった」理由 |
|---|---|---|
| 1 | colorscale stop を hex 専用で解釈 | Cividis がたまたま hex 側 |
| 2 | 輝度判定が両端2点のみ（**自己発見**）| cividis_r がたまたま全域単調 |
| 3 | ガンマ符号化のまま加重和（luma≠luminance）| 組込配色ではたまたま判定一致 |
| 4a | stop 間を見ていない | **3の修正が2の論拠を殺した** |
| 4b | 単調なだけの読めない配色が通る | cividis_r がたまたま 12.56:1 |
| 5 | 文書に失効した論拠が残存 | — |
| 6 | 同じ節の冒頭に別の古い記述 | — |
| 7 | 証人（`bluered_r`）が腐る書き方（**自己発見も同時**）| — |
| 8 | 観測値を不変条件のように記述 | — |

#### クラスA：「現行の配色ではたまたま正しく、本来の用途では壊れる」（1〜4b）

このテストの存在意義は**代替配色の評価**なのに、**ガードとして機能すべき場面でだけ壊れる**形だった。

- `mcolors.to_rgb` は `rgb(255,233,69)` を拒否する。Plotly は配色によって hex と `rgb()` を返し分け、
  **Cividis はたまたま hex 側**。別候補を評価した瞬間、配色の判定ではなく無関係に見える ValueError で死ぬ。
  「テストが壊れてる」と読まれ、配色の再考でなくテストの削除を招く。
- `0.2126R+0.7152G+0.0722B` をガンマ符号化された sRGB に当てるのは **luma であって relative luminance ではない**。
  `rgb(205,78,46)→rgb(242,34,36)` は naive で暗化・線形化で明化と**符号が逆になる**。
  組込配色では判定が一致するので現行バグではないが、手で作った暖色 ramp はまさにこの領域。
- 両端2点の判定は組込配色に破られる（plotly 6.7.0 実測 188ramp 中 26本）。最悪の `jet_r` は
  **両端の輝度差 0.070 に対し中間で 0.719 逆行**する＝「明→暗」を名乗るレインボー。

#### クラスB：「前の修正が別の箇所の前提を殺したのに、そこを追わなかった」（4a, 5, 6）

**これが一番怖い形で、3回踏んだ。**

ガンマ補正を入れた瞬間、「stop で単調なら全域で単調」（＝輝度が符号化 RGB の線形汎関数だから）が
偽になった。修正自体は正しいのに、それが前提にしていた別の記述を追わなかった。しかも
コードのコメントだけ直して文書を放置（5）、文書の一文だけ直して同じ節の冒頭を放置（6）、と繰り返した。

**根本原因**: CHANGELOG のテスト節を、修正のたびに**段落を継ぎ足して**いた。各段落が前の段落を
部分的に上書きする構造になり、どこか1つを直しても矛盾が残る。CLAUDE.md も同じで、1285文字の
一続きになって**文末が壊れていた**（`。` の直後に `、`）。

**対処**: 文単位の継ぎ当てをやめ、**節ごと「今どうなっているか」の記述に書き直す**。履歴は別表に分離。
論拠は定義箇所（`visualizer.py` 項）に一本化 — 同じ内容が2箇所にあれば片方だけ直す事故は必ず起きる。

#### クラスC：「証人が腐る」（7, 8）

**私の論理の誤り**。「将来 `bluered_r` が単調になったらサンプリング不要と知らせる」と書いたが、
サンプリングが要る根拠は**色空間の性質**（符号化 sRGB を線形補間した上に非線形＝凸な輝度変換を掛ける）で、
`bluered_r` の性質とは無関係。証人の失効を根拠の失効と書いていた。あの指示に従えば別配色で穴が開く。

**伝達関数の凸性から反例を構成し直した**：

```
rgb(0,192,0) → rgb(255,0,255)
  端点   0.3770 → 0.2848   (単調に見える)
  区間内 0.1421 まで沈んでから戻る
```

輝度は凸関数の重み付き和＝凸なので、暗い側の端点より下に沈んでから戻りうる。色空間の性質なので
ライブラリが配色を再定義しても腐らない。

同じ形を**名指しでなく走査**で洗った（`currently` / `today` / `今は` / `たまたま` / `not a live bug`）。
2件出た：`relative_luminance` の docstring が「現行配色では live bug でない」と書いて luma に戻す口実に
なっていた件、判別力表に失敗時の扱い（基準を緩めず期待値を更新する）が無かった件。
`plotly>=5.18` が上限無しなので、観測した件数を根拠に書くと依存更新で説明が食い違う——
**測定値には測定した版を併記**する形に揃えた。

### 最終的なガードの形

配色は名前で照合せず**実際に描かれる ramp を測色**して4性質を見る（`tests/colorimetry.py` に集約）。

| 性質 | 判定 | 落ちる例 |
|------|------|---------|
| 明→暗に単調 | stop 間まで密サンプル | `Blues_r` / `jet_r` / `bluered_r`（区間内のみ）|
| 情報を運ぶ | コントラスト比 ≥ 3:1（WCAG 1.4.11）| 近黒→黒（1.00:1）、`jet_r`（1.44:1）|
| 白と見分く | 薄肉端の HSV 彩度 > 0.5 | `bone_r`（低端が純白）|
| 2D と 3D が一致 | 定数の照合 | 3D だけ別配色 |

**輝度の式を2ファイルに手で書き写していて、両方が同じ間違いをしていた**のが集約の直接の理由。

変異注入はコピー上で実施（作業ツリー外）。定数を差し替える変異は**名前一致 assert が先に落ちて判定に
ならない**ので、基準そのものを既知スケールの期待値表で固定した。

### 教訓

1. **ガードのテストは、ガード対象の現在値に依存すると「機能すべき場面でだけ壊れる」。**
   このPRの実バグ5件は全部この形。テストの用途（代替候補の評価）で実際に動かして確かめる。
2. **修正したら、その修正が前提にしていた記述を走査する。** 3回踏んだ。コードのコメント・CHANGELOG・
   CLAUDE.md は別々に腐る。
3. **文書は継ぎ足すな、節ごと書き直せ。** 継ぎ足しは「各段落が前を部分的に上書きする」構造を作り、
   クラスBの温床になる。履歴が要るなら別の表に分離する。
4. **証人と根拠を分けて書く。** 「Xが成り立たなくなったらこの対策は不要」と書けるのは、
   対策の根拠が本当にXのときだけ。
5. **文字列置換には必ず assert を付ける。** テスト件数の更新が no-op で黙って失敗していた
   （`358 → 366` のまま、実際は 373）。これもクラスBの一種。
6. **レビューの収束判定は「深刻度の推移」で行う。** 実バグ→記述の食い違い→腐りやすさ、と
   単調に下がったのを見て切った。記述の精度を上げる余地は無限にあるので、
   **判定の正しさ**を基準にする。

### 次の作業（合意済み・未着手）

ゲート形状スペックの取り込み改善。要望は**アップロードの手間だけ**（比較はタブ複製＋画面分割で既に解決済み）。

- ラジオの「JSONアップロード」を **「ローカルから読込」に統合**。D&D 欄とパス欄を同じモードに並べる
- パス欄は **`isdir` / `isfile` で自動判別**（フォルダ→`*.json` 走査してドロップダウン、ファイル→直読み）。
  推測ではなくファイルシステムへの問い合わせなので安全
- **パス欄は `MFS_SPEC_DIR` が設定されている時だけ出す**。公開デプロイ（Streamlit Cloud）に
  任意ファイル読み取り口を付けないため。既定値も兼ねるのでローカルは操作ゼロ
- 両方埋まったら**アップロードが勝つ**（直近の明示的行為が勝つ）が、**どちらを使ったかを常に帯で明示**
- デモプリセット / JSON貼り付けは触らない
- 複数スペックを1ファイルに束ねる「セットファイル」案は**却下**。束を維持する義務が生まれ、
  小さな定期作業を別の定期作業に置き換えるだけ。「比較セットを他人に渡す」用途にだけ意味がある

**制約（絶対）**: 実図面由来の寸法が新しく住む場所を作らない。サーバ側保存は不可（Cloud のディスクは
再起動で消える＝黙ってデータを失う「偽の緑」、かつ公開ホストに実寸法が載る）。結果ZIPの
`settings.json` はスペックの名前と SHA-256 だけを記録して中身を載せない設計なので、そこに漏らさない。
